
The subject is an existing ticket body, and it may have gone stale in ways
that do not change the verdict: a renamed file, a moved symbol, a quoted
snippet that no longer matches the source.

If you score it 6 or above, ALSO return `description`: the same ticket,
rewritten against the code as it stands. It MUST carry every heading listed
under REQUIRED HEADINGS below, each with its `### ` marker, each alone on its
line, in that order.

The body you are shown may arrive FLATTENED. A ticket filed before this was
fixed reads back with its heading text on a bare line and no `### ` at all,
and with the backticks gone from its code spans. Restore them; do not assume
what you were shown is what was stored.

Keep what the ticket asks for — you are refreshing its evidence, not
renegotiating its scope. Correct every path, symbol and quotation to what is
there now, and say under `### Why` what had moved.

If you score it 5 or below, return `description` empty. A ticket that is
about to be retired is not worth rewriting.

Return `story` empty. Under a refresh the story is part of `description`:
where `### Why` already tells how the code behaves today and what goes wrong,
keep it and correct it against HEAD — and where it has none, add none. A
ticket already filed is refreshed, not rewritten into a newer shape.
