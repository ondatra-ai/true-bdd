# The scenario registry: every behaviour this repository is held to.
#
# Path from `documents.scenarios_yaml` in true-bdd/true-bdd.yaml.
#
# `build tests` walks it and asks whether each scenario's steps resolve to
# a step definition in the suite that owns it — the entry in
# the tests/<service>/ tree its `service:` names.
# pkg/testkit/bddgo then RUNS it: one Go subtest per scenario, steps
# bound by regexp to the definitions under that suite's path.
#
# Derived once from the tests/bdd-cli fixtures, which already were these
# scenarios in every respect but name. Hand-owned since; `us apply` is
# the writer for anything new.

metadata:
  title: Scenario registry
  version: '1.0'
  last_updated: '2026-08-16'
  description: Every behaviour the TrueBDD engine is held to, one entry per scenario. Derived once from the tests/bdd-cli fixtures; hand-owned since, with `us apply` as the writer for new entries.
scenarios:
  E2E-001:
    description: A command the spec states correctly but the machine cannot run must fail loudly, and on stdout
    service: bdd-cli
    path: tests/bdd-cli/build_code_test.go
    user_stories: []
    merged_steps:
      given:
        - the "build-code-command-not-executable" project tree
      when:
        - the System Architect runs "build code"
      then:
        - the command exits with code 1
        - 'stdout matches msg="Cannot run suite" framework=go-test'
        - stdout matches true-bdd-no-such-runner
        - stdout matches discovery failed
        - but: no file outside "tmp" changed
        - and: the engine spawned the test runner "true-bdd-no-such-runner"
        - and: the engine spawned exactly 1 test runner
        - and: the engine dispatched no AI turns
  E2E-002:
    description: A command that runs fine but cannot be parsed must be refused
    service: bdd-cli
    path: tests/bdd-cli/build_code_test.go
    user_stories: []
    merged_steps:
      given:
        - the "build-code-command-not-machine-readable" project tree
      when:
        - the System Architect runs "build code"
      then:
        - the command exits with code 1
        - 'stdout matches level=ERROR msg="Refusing to start'
        - stdout matches must emit machine-readable output
        - stdout matches is missing -json
        - stdout matches go-test
        - but: no file outside "tmp" changed
        - and: the engine spawned no test runner
        - and: the engine dispatched no AI turns
  E2E-003:
    description: '`build code` must run the command its architectural spec declares — and specifically the `replay` one'
    service: bdd-cli
    path: tests/bdd-cli/build_code_test.go
    user_stories: []
    merged_steps:
      given:
        - the "build-code-commands-from-spec" project tree
      when:
        - the System Architect runs "build code"
      then:
        - the command exits with code 0
        - stdout matches msg="Running tests" framework=go-test
        - stdout matches msg="Suite failures" failures=0 framework=go-test
        - 'stdout matches msg="Total failing tests across architecture" failures=0'
        - stdout matches BUILD CODE COMPLETE
        - but: no file outside "tmp" changed
        - and: the engine spawned a test runner with arguments matching -run \^TestGreen\$
        - and: the engine spawned exactly 1 test runner
        - and: the engine dispatched no AI turns
  E2E-004:
    description: A suite command that is present but names no program must be refused
    service: bdd-cli
    path: tests/bdd-cli/build_code_test.go
    user_stories: []
    merged_steps:
      given:
        - the "build-code-empty-command" project tree
      when:
        - the System Architect runs "build code"
      then:
        - the command exits with code 1
        - 'stdout matches go-test: test suite command is empty'
        - 'stdout matches test suite command is empty: \\"''''\\"'
        - stdout matches level=ERROR msg="Refusing to start" command="build code"
        - but: no file outside "tmp" changed
        - and: the engine spawned no test runner
        - and: the engine dispatched no AI turns
  E2E-005:
    description: After a fix is applied, `build code` must re-run THE TEST IT FIXED — not the whole suite
    service: bdd-cli
    path: tests/bdd-cli/build_code_test.go
    user_stories: []
    timeout: 15m
    merged_steps:
      given:
        - the "build-code-fix-named-test" project tree
        - the System Architect answers "1" to every prompt
      when:
        - the System Architect runs "build code --fix"
      then:
        - the command exits with code 0
        - stdout matches msg="Running tests" framework=go-test
        - stdout matches msg="Suite failures" failures=1 framework=go-test
        - 'stdout matches msg="Total failing tests across architecture" failures=1'
        - stdout matches msg="Fix applied; re-running this test in isolation"
        - stdout matches BUILD CODE COMPLETE
        - and: the file "services/calc/calc.go" is modified
        - and: the file "tests/suite/suite-runs.log" has exactly 1 line
        - and: the engine spawned a test runner with arguments matching TestAddSumsItsArguments
        - but: the file "tests/suite/add_test.go" is unchanged
        - and: the file "tests/suite/canary_test.go" is unchanged
        - and: the file "docs/architecture/architecture.yaml" is unchanged
        # Independent outcome check: the harness runs the suite itself in
        # the tree the fix loop left. Last, so the run it appends to
        # suite-runs.log lands after the line-count clause and both snapshots.
        - and: the command "go test -json -count=1 ./tests/suite/..." run in "." exits with code 0
  E2E-006:
    description: A spec the loader cannot decode must be refused with the file named
    service: bdd-cli
    path: tests/bdd-cli/build_code_test.go
    user_stories: []
    merged_steps:
      given:
        - the "build-code-malformed-spec" project tree
      when:
        - the System Architect runs "build code"
      then:
        - the command exits with code 1
        - 'stdout matches level=ERROR msg="Refusing to start'
        - stdout matches failed to parse architecture file
        - stdout matches docs/architecture/architecture\.yaml
        - but: no file outside "tmp" changed
        - and: the file "docs/architecture/architecture.yaml" is unchanged
        - and: the engine spawned no test runner
        - and: the engine dispatched no AI turns
  E2E-007:
    description: A test suite that does not say how to run itself must be refused, not run some other way
    service: bdd-cli
    path: tests/bdd-cli/build_code_test.go
    user_stories: []
    merged_steps:
      given:
        - the "build-code-missing-suite-command" project tree
      when:
        - the System Architect runs "build code"
      then:
        - the command exits with code 1
        - 'stdout matches level=ERROR msg="Refusing to start'
        - stdout matches commands\.replay
        - stdout matches architecture\.testing\[go-test\]
        - but: no file outside "tmp" changed
        - and: the engine spawned no test runner
        - and: the engine dispatched no AI turns
  E2E-008:
    description: An architectural spec that declares no services at all must be refused, not walked to a trivial success
    service: bdd-cli
    path: tests/bdd-cli/build_code_test.go
    user_stories: []
    merged_steps:
      given:
        - the "build-code-no-services" project tree
      when:
        - the System Architect runs "build code"
      then:
        - the command exits with code 1
        - 'stdout matches level=ERROR msg="Refusing to start'
        - stdout matches no services to walk
        - but: no file outside "tmp" changed
        - and: the engine spawned no test runner
        - and: the engine dispatched no AI turns
  E2E-009:
    description: An architectural spec that never says how its tests run must be refused, not walked to a trivial success
    service: bdd-cli
    path: tests/bdd-cli/build_code_test.go
    user_stories: []
    merged_steps:
      given:
        - the "build-code-no-suites" project tree
      when:
        - the System Architect runs "build code"
      then:
        - the command exits with code 1
        - 'stdout matches level=ERROR msg="Refusing to start'
        - stdout matches declares no testing framework
        - but: no file outside "tmp" changed
        - and: the engine spawned no test runner
        - and: the engine dispatched no AI turns
  E2E-010:
    description: A walk that ends with a test still failing must exit non-zero
    service: bdd-cli
    path: tests/bdd-cli/build_code_test.go
    user_stories: []
    merged_steps:
      given:
        - the "build-code-nonconvergence" project tree
      when:
        - the System Architect runs "build code"
      then:
        - the command exits with code 1
        - stdout matches msg="Running tests" framework=go-test
        - stdout matches msg="Suite failures" failures=1 framework=go-test
        - 'stdout matches msg="Total failing tests across architecture" failures=1'
        - stdout matches BUILD CODE COMPLETE
        - but: no file outside "tmp" changed
        - and: the engine spawned the test runner "go"
        - and: the engine dispatched 1 AI turn
  E2E-011:
    description: A Playwright suite whose app does not exist yet must drive the fix loop until the browser assertion passes
    service: bdd-cli
    path: tests/bdd-cli/build_code_test.go
    user_stories: []
    timeout: 25m
    merged_steps:
      given:
        - the "build-code-playwright-nextjs" project tree
        - and: the project's browser test dependencies are installed
        - the System Architect answers "1" to every prompt
      when:
        - the System Architect runs "build code --fix"
      then:
        - the command exits with code 0
        - stdout matches msg="Running tests" framework=playwright
        - stdout matches BUILD CODE COMPLETE
        - and: the file "services/frontend/Dockerfile" is created
        - and: the file "services/frontend/package.json" is created
        - but: the file "docker-compose.yaml" is unchanged
        - and: the file "tests/integration/frontend-home.spec.ts" is unchanged
        - and: the file "docs/architecture/architecture.yaml" is unchanged
        - and: no file outside "tmp" and "services/frontend" changed
        # THE assertion this scenario exists for: the harness runs the
        # declared browser suite itself, against the app the loop produced.
        # Every file clause above is a proxy for this one.
        - and: the command "npx playwright test --reporter=json --config playwright.config.ts integration" run in "tests" exits with code 0
        - and: 'judge: services/frontend/package.json declares Next.js and React, and defines whichever of the dev or start scripts the created Dockerfile invokes to launch the server'
        - and: 'judge: services/frontend/Dockerfile defines a runnable Next.js build — it installs the declared dependencies, exposes the port docker-compose.yaml maps, and starts the server'
  E2E-012:
    description: A framework the engine cannot route to must be refused, not walked
    service: bdd-cli
    path: tests/bdd-cli/build_code_test.go
    user_stories: []
    merged_steps:
      given:
        - the "build-code-unknown-framework" project tree
      when:
        - the System Architect runs "build code"
      then:
        - the command exits with code 1
        - 'stdout matches level=ERROR msg="Refusing to start'
        - stdout matches unknown test framework
        - stdout matches rspec
        - but: no file outside "tmp" changed
        - and: the engine spawned no test runner
        - and: the engine dispatched no AI turns
  E2E-014:
    description: A command whose quoting never closes must be refused, not guessed
    service: bdd-cli
    path: tests/bdd-cli/build_code_test.go
    user_stories: []
    merged_steps:
      given:
        - the "build-code-unterminated-quote" project tree
      when:
        - the System Architect runs "build code"
      then:
        - the command exits with code 1
        - 'stdout matches level=ERROR msg="Refusing to start'
        - stdout matches unterminated quote
        - stdout matches go-test
        - but: no file outside "tmp" changed
        - and: the engine spawned no test runner
        - and: the engine dispatched no AI turns
  E2E-015:
    description: A registry that parses but holds no scenarios must be refused, never walked to a trivial success
    service: bdd-cli
    path: tests/bdd-cli/build_tests_test.go
    user_stories: []
    merged_steps:
      given:
        - the "build-tests-empty-registry" project tree
      when:
        - the System Architect runs "build tests"
      then:
        - the command exits with code 1
        - stdout matches BUILD TESTS
        - 'stdout matches docs/scenarios\.yaml: requirements registry has no scenarios to walk'
        - stdout matches level=ERROR msg="Refusing to start" command="build tests"
        - but: no file outside "tmp" changed
        - and: the engine dispatched no AI turns
  E2E-016:
    description: A registry scenario with no test must get a generated test file and a step definition for every step it declares
    service: bdd-cli
    path: tests/bdd-cli/build_tests_test.go
    user_stories: []
    timeout: 15m
    merged_steps:
      given:
        - the "build-tests-fix-happy-path" project tree
        - the System Architect answers "1" to every prompt
      when:
        - the System Architect runs "build tests --fix"
      then:
        - the command exits with code 0
        - and: the file "tests/mcp/mcp_test.go" is created
        - and: the file "tests/mcp/mcp_test.go" matches // Code generated by `true-bdd build tests`\. DO NOT EDIT\.
        - and: the file "tests/mcp/mcp_test.go" matches scenarios\.New\(t, "INT-900"\)
        - and: exactly 1 file matching "tests/mcp/steps/*.go" is created
        - and: every step of scenario "INT-900" in "docs/scenarios.yaml" is matched by a step definition under "tests/mcp/steps"
        - but: the file "docs/scenarios.yaml" is unchanged
        - and: no file outside "tmp" and "tests" changed
  E2E-017:
    description: A registry that is valid YAML but the wrong SHAPE must be refused with the decoder's own complaint, not a generic parse failure
    service: bdd-cli
    path: tests/bdd-cli/build_tests_test.go
    user_stories: []
    merged_steps:
      given:
        - the "build-tests-malformed-registry" project tree
      when:
        - the System Architect runs "build tests"
      then:
        - the command exits with code 1
        - stdout matches BUILD TESTS
        - stdout matches cannot unmarshal !!seq into map\[string\]
        - stdout matches level=ERROR msg="Refusing to start" command="build tests"
        - but: no file outside "tmp" changed
        - and: the file "docs/scenarios.yaml" is unchanged
        - and: the engine dispatched no AI turns
  E2E-018:
    description: A host that never declared where its scenario registry lives must be refused, never quietly served the conventional path
    service: bdd-cli
    path: tests/bdd-cli/build_tests_test.go
    user_stories: []
    merged_steps:
      given:
        - the "build-tests-unconfigured-registry" project tree
      when:
        - the System Architect runs "build tests"
      then:
        - the command exits with code 1
        - 'stdout matches level=ERROR msg="Refusing to start'
        - stdout matches documents\.scenarios_yaml
        - but: no file outside "tmp" changed
        - and: the file "docs/scenarios.yaml" is unchanged
        - and: the engine dispatched no AI turns
  E2E-019:
    description: Multi-provider routing must fail LOUDLY, never quietly
    service: bdd-cli
    path: tests/bdd-cli/build_tests_test.go
    user_stories: []
    merged_steps:
      given:
        - the "build-tests-unknown-cli-tier" project tree
      when:
        - the System Architect runs "build tests"
      then:
        - the command exits with code 1
        - but: no file outside "tmp" changed
        - and: the file "docs/scenarios.yaml" is unchanged
        - and: the engine dispatched no AI turns
  E2E-020:
    description: --help must print the command tree and change nothing on disk
    service: bdd-cli
    path: tests/bdd-cli/cli_test.go
    user_stories: []
    merged_steps:
      given:
        - the "help-flag" project tree
      when:
        - the System Architect runs "--help"
      then:
        - the command exits with code 0
        - stdout matches TrueBDD CLI
        - 'stdout matches Usage:'
        - stdout matches true-bdd \[command\]
        - 'stdout matches Available Commands:'
        - stdout matches \bbuild\b
        - stdout matches \bus\b
        - stdout matches -h, --help
        - no file outside "tmp" changed
  E2E-021:
    description: Two registry entries claiming the same acceptance criterion must collapse into one, leaving every other entry untouched
    service: bdd-cli
    path: tests/bdd-cli/us_apply_test.go
    user_stories: []
    timeout: 20m
    merged_steps:
      given:
        - the "us-apply-duplicate-collapse" project tree
        - the Product Owner answers "1" to every prompt
      when:
        - the Product Owner runs "us apply 95.1 --fix"
      then:
        - the command exits with code 0
        - 'stdout matches msg=AC index=1 total=1'
        - 'stdout matches msg="RE-WALK \(fixes applied, verifying\)" attempt=2'
        - stdout matches ALL CHECKS PASSED!
        - stdout matches APPLY COMPLETE
        - and: the file "docs/scenarios.yaml" is modified
        - and: exactly 1 scenario in "docs/scenarios.yaml" comes from acceptance criterion "95.1-001"
        - and: the scenario from acceptance criterion "95.1-001" in "docs/scenarios.yaml" cites the story "docs/product/stories/95.1-duplicate-collapse.yaml"
        - and: the scenario from acceptance criterion "95.1-001" in "docs/scenarios.yaml" matches HTTP 200
        - and: exactly 1 scenario in "docs/scenarios.yaml" comes from acceptance criterion "90.1-001"
        - but: the file "docs/product/stories/95.1-duplicate-collapse.yaml" is unchanged
        - and: the only file changed outside "tmp" is "docs/scenarios.yaml"
        - and: stdout does not match Hit max apply attempts
        - and: 'judge: the scenario from acceptance criterion 90.1-001 — the unrelated home-page welcome banner — still says what the seed registry said it did'
  E2E-022:
    description: A story carried forward from the older `scenarios.test_scenarios[]` layout must be refused, and told what to convert
    service: bdd-cli
    path: tests/bdd-cli/us_apply_test.go
    user_stories: []
    merged_steps:
      given:
        - the "us-apply-malformed-scenarios" project tree
      when:
        - the Product Owner runs "us apply 95.5"
      then:
        - the command exits with code 1
        - stdout matches US APPLY — Story 95\.5
        - stdout matches msg="Seeded scratch registry"
        - stdout matches us apply requires acceptance_criteria with embedded steps
        - stdout matches docs/product/stories/95\.5-legacy-test-scenarios\.yaml
        - stdout matches level=ERROR msg="Refusing to start" command="us apply"
        - but: no file outside "tmp" changed
        - and: the file "docs/scenarios.yaml" is unchanged
        - and: the file "docs/product/stories/95.5-legacy-test-scenarios.yaml" is unchanged
        - and: the engine dispatched no AI turns
  E2E-023:
    description: '`us apply` on a story number that has no file must be refused, and the refusal must say what filename it looked for'
    service: bdd-cli
    path: tests/bdd-cli/us_apply_test.go
    user_stories: []
    merged_steps:
      given:
        - the "us-apply-missing-story" project tree
      when:
        - the Product Owner runs "us apply 95.4"
      then:
        - the command exits with code 1
        - stdout matches US APPLY — Story 95\.4
        - stdout matches msg="Seeded scratch registry"
        - 'stdout matches in docs/product/stories \(expected format: 95\.4-<slug>\.yaml\)'
        - stdout matches level=ERROR msg="Refusing to start" command="us apply"
        - but: no file outside "tmp" changed
        - and: the file "docs/scenarios.yaml" is unchanged
        - and: the file "docs/product/stories/95.1-duplicate-collapse.yaml" is unchanged
        - and: the engine dispatched no AI turns
  E2E-024:
    description: Every location a host project uses is supposed to come from `true-bdd.yaml` — never a hardcoded path
    service: bdd-cli
    path: tests/bdd-cli/us_apply_test.go
    user_stories: []
    timeout: 20m
    merged_steps:
      given:
        - the "us-apply-relocated-registry" project tree
        - the Product Owner answers "1" to every prompt
      when:
        - the Product Owner runs "us apply 95.2 --fix"
      then:
        - the command exits with code 0
        - 'stdout matches msg=AC index=1 total=1'
        - 'stdout matches msg="RE-WALK \(fixes applied, verifying\)" attempt=2'
        - stdout matches ALL CHECKS PASSED!
        - stdout matches APPLY COMPLETE
        - stdout matches msg="All ACs passed; updated from scratch" file=\./docs/specs/requirements\.yaml
        - and: the file "docs/specs/requirements.yaml" is modified
        - and: exactly 1 scenario in "docs/specs/requirements.yaml" comes from acceptance criterion "95.2-001"
        - and: the scenario from acceptance criterion "95.2-001" in "docs/specs/requirements.yaml" cites the story "docs/product/stories/95.2-relocated-registry.yaml"
        - and: the scenario from acceptance criterion "95.2-001" in "docs/specs/requirements.yaml" matches /health
        - and: exactly 1 scenario in "docs/specs/requirements.yaml" comes from acceptance criterion "90.2-001"
        - but: no file matching "docs/scenarios.yaml" is created
        - and: the file "docs/product/stories/95.2-relocated-registry.yaml" is unchanged
        - and: 'judge: the scenario from acceptance criterion 90.2-001 — the unrelated settings page — still says what the seed registry said it did'
  E2E-025:
    description: A story whose acceptance criteria are all missing from the registry must re-walk after the merge and end at a fixpoint
    service: bdd-cli
    path: tests/bdd-cli/us_apply_test.go
    user_stories: []
    timeout: 20m
    merged_steps:
      given:
        - the "us-apply-rewalk-converges" project tree
        - and: the "us-apply" checklist is narrowed to its "already appear as a scenario in the registry" prompt
        - the Product Owner answers "1" to every prompt
      when:
        - the Product Owner runs "us apply 99.3 --fix"
      then:
        - the command exits with code 0
        - 'stdout matches msg=AC index=1 total=2'
        - 'stdout matches msg=AC index=2 total=2'
        - 'stdout matches msg="RE-WALK \(fixes applied, verifying\)" attempt=2'
        - stdout matches ALL CHECKS PASSED!
        - stdout matches APPLY COMPLETE
        - and: the file "docs/scenarios.yaml" is modified
        - and: exactly 1 scenario in "docs/scenarios.yaml" comes from acceptance criterion "99.3-001"
        - and: exactly 1 scenario in "docs/scenarios.yaml" comes from acceptance criterion "99.3-002"
        - and: no two scenarios in "docs/scenarios.yaml" share the same steps
        - but: the file "docs/product/stories/99.3-rewalk-fixture.yaml" is unchanged
        - and: the only file changed outside "tmp" is "docs/scenarios.yaml"
        - and: stdout does not match RE-WALK 3/
        - and: stdout does not match Hit max apply attempts
        - and: 'judge: each scenario added to the registry carries the given, when and then steps of the acceptance criterion it came from in docs/product/stories/99.3-rewalk-fixture.yaml'
  E2E-026:
    description: '`us apply` on a story that was created but never refined must be refused, not walked to a trivial success'
    service: bdd-cli
    path: tests/bdd-cli/us_apply_test.go
    user_stories: []
    merged_steps:
      given:
        - the "us-apply-story-without-acs" project tree
      when:
        - the Product Owner runs "us apply 95.3"
      then:
        - the command exits with code 1
        - stdout matches US APPLY — Story 95\.3
        - stdout matches msg="Seeded scratch registry"
        - 'stdout matches docs/product/stories/95\.3-document-summary-created-not-refined\.yaml: story has no acceptance_criteria entries to apply'
        - stdout matches level=ERROR msg="Refusing to start" command="us apply"
        - but: no file outside "tmp" changed
        - and: the file "docs/scenarios.yaml" is unchanged
        - and: the file "docs/product/stories/95.3-document-summary-created-not-refined.yaml" is unchanged
        - and: the engine dispatched no AI turns
  E2E-027:
    description: Two files claiming the same epic number must be refused, not silently resolved by taking the first glob match
    service: bdd-cli
    path: tests/bdd-cli/us_create_test.go
    user_stories: []
    merged_steps:
      given:
        - the "us-create-ambiguous-epic" project tree
      when:
        - the Product Owner runs "us create 92.1"
      then:
        - the command exits with code 1
        - stdout matches US CREATE — Story 92\.1
        - stdout matches failed to load epic 92
        - stdout matches multiple epic files found matching pattern docs/product/epics/epic-92-\*\.yaml
        - stdout matches \[docs/product/epics/epic-92-document-intelligence\.yaml docs/product/epics/epic-92-summaries\.yaml\]
        - stdout matches level=ERROR msg="Refusing to start" command="us create"
        - but: no file outside "tmp" changed
        - and: no file matching "docs/product/stories/*.yaml" is created
        - and: the engine dispatched no AI turns
  E2E-028:
    description: A story number that is not `<epic>.<story>` must be refused before anything is read
    service: bdd-cli
    path: tests/bdd-cli/us_create_test.go
    user_stories: []
    merged_steps:
      given:
        - the "us-create-bad-story-number" project tree
      when:
        - the Product Owner runs "us create 4"
      then:
        - the command exits with code 1
        - 'stdout matches expected <epic>\.<story>, got \\"4\\"'
        - stdout matches level=ERROR msg="Refusing to start" command="us create"
        - but: no file outside "tmp" changed
        - and: no file matching "docs/product/stories/*.yaml" is created
        - and: the engine dispatched no AI turns
  E2E-029:
    description: A `--fix` run must be refused when any prompt it would walk carries no `F:`, rather than spending a fix turn that has nothing to say
    service: bdd-cli
    path: tests/bdd-cli/us_create_test.go
    user_stories: []
    merged_steps:
      given:
        - the "us-create-unfixable-checklist" project tree
      when:
        - the Product Owner runs "us create 99.1 --fix"
      then:
        - the command exits with code 1
        - 'stdout matches us-create/format #1 Does the story follow the format'
        - stdout matches level=ERROR msg="Refusing to start" command="us create"
        - but: no file outside "tmp" changed
        - and: no file matching "docs/product/stories/*.yaml" is created
        - and: the engine dispatched no AI turns
  E2E-030:
    description: A story seed that passes every check must be written to the stories directory as one new file
    service: bdd-cli
    path: tests/bdd-cli/us_create_test.go
    user_stories: []
    timeout: 20m
    merged_steps:
      given:
        - the "us-create-happy-path" project tree
      when:
        - the Product Owner runs "us create 99.1"
      then:
        - the command exits with code 0
        - and: exactly 1 file matching "docs/product/stories/*.yaml" is created
        - and: exactly 1 file matching "docs/product/stories/99.1-*.yaml" is created
        - and: the story "docs/product/stories/99.1-*.yaml" has id "99.1"
        - and: the story "docs/product/stories/99.1-*.yaml" has at least 3 acceptance criteria
        - and: every acceptance criterion in the story "docs/product/stories/99.1-*.yaml" has an id matching ^AC-\d+$ and a non-empty description
        - but: the file "docs/product/epics/epic-99-bdd-test.yaml" is unchanged
        - and: no file outside "tmp" and "docs/product/stories" changed
        - and: 'judge: the story''s as_a, i_want and so_that clauses keep the meaning of the epic''s — a Claude User who wants a short summary of a Google Doc on request, so as not to have to download it or copy sections of it into a separate tool just to get a quick gist'
        - and: 'judge: the acceptance criteria cover the substance of the epic''s three — summarising a shared Google Doc, the error path when the document is not shared, and the partial-summary path when it is too long — in any order, with polished wording, and with extra criteria allowed'
  E2E-031:
    description: A story seed with no as_a clause and a generic so_that must fail both checks and write no story file
    service: bdd-cli
    path: tests/bdd-cli/us_create_test.go
    user_stories: []
    timeout: 20m
    merged_steps:
      given:
        - the "us-create-missing-clause" project tree
      when:
        - the Product Owner runs "us create 97.1"
      then:
        - the command exits with code 0
        - stdout matches Validation failed. Use --fix flag to enter interactive fix mode.
        - stdout matches CREATE COMPLETE
        - but: no file outside "tmp" changed
        - and: no file matching "docs/product/stories/*.yaml" is created
        - and: stdout does not match ALL CHECKS PASSED!
        - and: 'stdout does not match Story saved to:'
  E2E-032:
    description: A checklist prompt may only ask for evidence the engine can actually hand it
    service: bdd-cli
    path: tests/bdd-cli/us_create_test.go
    user_stories: []
    merged_steps:
      given:
        - the "us-create-missing-document" project tree
      when:
        - the Product Owner runs "us create 99.1"
      then:
        - the command exits with code 1
        - stdout matches docs/product/product\.yaml
        - 'stdout matches level=ERROR msg="Refusing to start: checklist documents unsatisfiable" command="us create" checklist=us-create'
        - but: no file outside "tmp" changed
        - and: no file matching "docs/product/stories/*.yaml" is created
        - and: the engine dispatched no AI turns
  E2E-033:
    description: An epic number with no file must be refused, and the refusal must show the filename pattern that found nothing
    service: bdd-cli
    path: tests/bdd-cli/us_create_test.go
    user_stories: []
    merged_steps:
      given:
        - the "us-create-missing-epic" project tree
      when:
        - the Product Owner runs "us create 93.1"
      then:
        - the command exits with code 1
        - stdout matches US CREATE — Story 93\.1
        - stdout matches failed to load epic 93
        - stdout matches no epic file found matching pattern docs/product/epics/epic-93-\*\.yaml
        - stdout matches level=ERROR msg="Refusing to start" command="us create"
        - but: no file outside "tmp" changed
        - and: no file matching "docs/product/stories/*.yaml" is created
        - and: the file "docs/product/epics/epic-99-bdd-test.yaml" is unchanged
        - and: the engine dispatched no AI turns
  E2E-034:
    description: Asking for a story index the epic does not have must be refused with both numbers in the message
    service: bdd-cli
    path: tests/bdd-cli/us_create_test.go
    user_stories: []
    merged_steps:
      given:
        - the "us-create-story-out-of-range" project tree
      when:
        - the Product Owner runs "us create 94.7"
      then:
        - the command exits with code 1
        - stdout matches US CREATE — Story 94\.7
        - stdout matches story index 7 out of bounds for epic 94 \(has 2 stories\)
        - stdout matches level=ERROR msg="Refusing to start" command="us create"
        - but: no file outside "tmp" changed
        - and: no file matching "docs/product/stories/*.yaml" is created
        - and: the engine dispatched no AI turns
  E2E-035:
    description: A story seed defective in every checked dimension must report each failure and write no story file
    service: bdd-cli
    path: tests/bdd-cli/us_create_test.go
    user_stories: []
    timeout: 20m
    merged_steps:
      given:
        - the "us-create-validation-fails" project tree
      when:
        - the Product Owner runs "us create 98.1"
      then:
        - the command exits with code 0
        - stdout matches Validation failed. Use --fix flag to enter interactive fix mode.
        - stdout matches CREATE COMPLETE
        - but: no file outside "tmp" changed
        - and: no file matching "docs/product/stories/*.yaml" is created
        - and: stdout does not match ALL CHECKS PASSED!
        - and: 'stdout does not match Story saved to:'
  E2E-036:
    description: Two files claiming the same story id must be refused, not silently resolved by picking one
    service: bdd-cli
    path: tests/bdd-cli/us_refine_test.go
    user_stories: []
    merged_steps:
      given:
        - the "us-refine-ambiguous-story" project tree
      when:
        - the Product Owner runs "us refine 96.9"
      then:
        - the command exits with code 1
        - stdout matches US REFINE — Story 96\.9
        - stdout matches level=ERROR msg="Multiple story files found" story_number=96\.9 count=2
        - stdout matches multiple story files found for story 96\.9
        - stdout matches 96\.9-document-summary\.yaml
        - stdout matches 96\.9-document-summary-v2\.yaml
        - stdout matches level=ERROR msg="Refusing to start" command="us refine"
        - but: no file outside "tmp" changed
        - and: the file "docs/product/stories/96.9-document-summary.yaml" is unchanged
        - and: the file "docs/product/stories/96.9-document-summary-v2.yaml" is unchanged
        - and: the engine dispatched no AI turns
  E2E-037:
    description: A vague qualifier in an acceptance criterion's description must drive the fix loop until the description is measurable
    service: bdd-cli
    path: tests/bdd-cli/us_refine_test.go
    user_stories: []
    timeout: 15m
    merged_steps:
      given:
        - the "us-refine-fix-desc-qualifier" project tree
        - and: the "us-refine" checklist is narrowed to its "whether its description field contains a vague word" prompt
        - the Product Owner answers "1" to every prompt
      when:
        - the Product Owner runs "us refine 96.6 --fix"
      then:
        - the command exits with code 0
        - stdout matches ALL CHECKS PASSED!
        - 'stdout matches msg="Story saved"'
        - stdout matches REFINE COMPLETE
        - and: the only file changed outside "tmp" is "docs/product/stories/96.6-document-summary-desc-qualifier.yaml"
        - and: the story "docs/product/stories/96.6-document-summary-desc-qualifier.yaml" has id "96.6"
        - and: the story "docs/product/stories/96.6-document-summary-desc-qualifier.yaml" has 4 acceptance criteria
        - and: every acceptance criterion in the story "docs/product/stories/96.6-document-summary-desc-qualifier.yaml" has given, when and then steps
        - and: the description of acceptance criterion "AC-3" of the story "docs/product/stories/96.6-document-summary-desc-qualifier.yaml" does not match (?i)\b(properly|correctly|appropriate|user-friendly|fast|quickly|easily|seamless|intuitive)\b
        - and: stdout does not match Hit max apply attempts
        - and: 'judge: the four acceptance criteria still cover the substance of the seed — a 150-word summary, the unshared-document message, the timeout message, and the first-10000-words notice — with every exact message and numeric threshold intact'
  E2E-038:
    description: An acceptance criterion described in Given/When/Then prose must drive the fix loop until it states a rule
    service: bdd-cli
    path: tests/bdd-cli/us_refine_test.go
    user_stories: []
    timeout: 15m
    merged_steps:
      given:
        - the "us-refine-fix-descriptions" project tree
        - and: the "us-refine" checklist is narrowed to its "whether its description field follows the rule-based format" prompt
        - the Product Owner answers "1" to every prompt
      when:
        - the Product Owner runs "us refine 96.2 --fix"
      then:
        - the command exits with code 0
        - stdout matches ALL CHECKS PASSED!
        - 'stdout matches msg="Story saved"'
        - stdout matches REFINE COMPLETE
        - and: the only file changed outside "tmp" is "docs/product/stories/96.2-document-summary-descriptions.yaml"
        - and: the story "docs/product/stories/96.2-document-summary-descriptions.yaml" has id "96.2"
        - and: the story "docs/product/stories/96.2-document-summary-descriptions.yaml" has 5 acceptance criteria
        - and: every acceptance criterion in the story "docs/product/stories/96.2-document-summary-descriptions.yaml" has given, when and then steps
        - and: the description of acceptance criterion "AC-1" of the story "docs/product/stories/96.2-document-summary-descriptions.yaml" matches (?i)\b(must|should)\b
        - and: the description of acceptance criterion "AC-1" of the story "docs/product/stories/96.2-document-summary-descriptions.yaml" does not match (?i)\bwhen\b.*\bthen\b
        - and: stdout does not match Hit max apply attempts
        - and: 'judge: the five acceptance criteria still cover the substance of the seed — a 150-word summary, the unshared-document message, the timeout message, the first-10000-words notice, and the section-heading outline — with every exact message and numeric threshold intact'
  E2E-039:
    description: A forbidden action verb in a step must drive the fix loop until the step names an observable action
    service: bdd-cli
    path: tests/bdd-cli/us_refine_test.go
    user_stories: []
    timeout: 15m
    merged_steps:
      given:
        - the "us-refine-fix-forbidden-verb" project tree
        - and: the "us-refine" checklist is narrowed to its "use action verbs from the forbidden_actions list" prompt
        - the Product Owner answers "1" to every prompt
      when:
        - the Product Owner runs "us refine 96.8 --fix"
      then:
        - the command exits with code 0
        - stdout matches ALL CHECKS PASSED!
        - 'stdout matches msg="Story saved"'
        - stdout matches REFINE COMPLETE
        - and: the only file changed outside "tmp" is "docs/product/stories/96.8-document-summary-forbidden-verb.yaml"
        - and: the story "docs/product/stories/96.8-document-summary-forbidden-verb.yaml" has id "96.8"
        - and: the story "docs/product/stories/96.8-document-summary-forbidden-verb.yaml" has 4 acceptance criteria
        - and: every acceptance criterion in the story "docs/product/stories/96.8-document-summary-forbidden-verb.yaml" has given, when and then steps
        - and: the steps of acceptance criterion "AC-1" of the story "docs/product/stories/96.8-document-summary-forbidden-verb.yaml" do not match (?i)\b(handle|handles|process|parse)\b
        - and: stdout does not match Hit max apply attempts
        - and: 'judge: AC-1''s then step now states a concrete observable action in place of the forbidden verb, and keeps the 150-word threshold; the four criteria still cover the substance of the seed with every exact message intact'
  E2E-040:
    description: A vague qualifier in a step must drive the fix loop until the step is measurable
    service: bdd-cli
    path: tests/bdd-cli/us_refine_test.go
    user_stories: []
    timeout: 15m
    merged_steps:
      given:
        - the "us-refine-fix-step-qualifier" project tree
        - and: the "us-refine" checklist is narrowed to its "whether its steps field contains a vague word" prompt
        - the Product Owner answers "1" to every prompt
      when:
        - the Product Owner runs "us refine 96.7 --fix"
      then:
        - the command exits with code 0
        - stdout matches ALL CHECKS PASSED!
        - 'stdout matches msg="Story saved"'
        - stdout matches REFINE COMPLETE
        - and: the only file changed outside "tmp" is "docs/product/stories/96.7-document-summary-step-qualifier.yaml"
        - and: the story "docs/product/stories/96.7-document-summary-step-qualifier.yaml" has id "96.7"
        - and: the story "docs/product/stories/96.7-document-summary-step-qualifier.yaml" has 4 acceptance criteria
        - and: every acceptance criterion in the story "docs/product/stories/96.7-document-summary-step-qualifier.yaml" has given, when and then steps
        - and: the steps of acceptance criterion "AC-4" of the story "docs/product/stories/96.7-document-summary-step-qualifier.yaml" do not match (?i)\b(properly|correctly|appropriate|user-friendly|fast|quickly|easily|seamless|intuitive)\b
        - and: stdout does not match Hit max apply attempts
        - and: 'judge: AC-4''s then step still states the first-10000-words notice as an observable outcome, and the four criteria still cover the substance of the seed with every exact message and numeric threshold intact'
  E2E-041:
    description: An acceptance criterion with no steps must drive the fix loop until it carries Given/When/Then
    service: bdd-cli
    path: tests/bdd-cli/us_refine_test.go
    user_stories: []
    timeout: 15m
    merged_steps:
      given:
        - the "us-refine-fix-steps" project tree
        - and: the "us-refine" checklist is narrowed to its "whether its steps field follows the Given/When/Then format" prompt
        - the Product Owner answers "1" to every prompt
      when:
        - the Product Owner runs "us refine 96.5 --fix"
      then:
        - the command exits with code 0
        - stdout matches ALL CHECKS PASSED!
        - 'stdout matches msg="Story saved"'
        - stdout matches REFINE COMPLETE
        - and: the only file changed outside "tmp" is "docs/product/stories/96.5-document-summary-steps.yaml"
        - and: the story "docs/product/stories/96.5-document-summary-steps.yaml" has id "96.5"
        - and: the story "docs/product/stories/96.5-document-summary-steps.yaml" has 5 acceptance criteria
        - and: every acceptance criterion in the story "docs/product/stories/96.5-document-summary-steps.yaml" has given, when and then steps
        - and: stdout does not match Hit max apply attempts
        - and: 'judge: AC-2''s new given, when and then steps are derived from its own description, and the five criteria still cover the substance of the seed with every exact message and numeric threshold intact'
  E2E-042:
    description: A story whose acceptance criteria already pass every check must converge on the first walk and be rewritten in place
    service: bdd-cli
    path: tests/bdd-cli/us_refine_test.go
    user_stories: []
    timeout: 25m
    merged_steps:
      given:
        - the "us-refine-happy-path" project tree
      when:
        - the Product Owner runs "us refine 96.1"
      then:
        - the command exits with code 0
        - stdout matches ALL CHECKS PASSED!
        - 'stdout matches msg="Story saved"'
        - stdout matches REFINE COMPLETE
        - and: the only file changed outside "tmp" is "docs/product/stories/96.1-document-summary-refined.yaml"
        - and: the story "docs/product/stories/96.1-document-summary-refined.yaml" has id "96.1"
        - and: the story "docs/product/stories/96.1-document-summary-refined.yaml" has 4 acceptance criteria
        - and: every acceptance criterion in the story "docs/product/stories/96.1-document-summary-refined.yaml" has given, when and then steps
        - and: 'judge: the four acceptance criteria still cover the substance of the seed — a summary of 150 words or fewer for a shared document, the "This document is not shared with your account" message, the "The summary timed out - please try again" message at 30 seconds, and summarising only the first 10000 words — with every exact message and number intact'
        - and: 'judge: the as_a, i_want and so_that clauses keep their meaning — a Claude User wanting a short summary of a shared Google Doc on request, so as to decide whether it needs attention without reading it in full'
  E2E-043:
    description: A story file that is not valid YAML must be refused with the parser's own complaint, not a generic failure
    service: bdd-cli
    path: tests/bdd-cli/us_refine_test.go
    user_stories: []
    merged_steps:
      given:
        - the "us-refine-malformed-story" project tree
      when:
        - the Product Owner runs "us refine 96.9"
      then:
        - the command exits with code 1
        - stdout matches US REFINE — Story 96\.9
        - stdout matches level=ERROR msg="Failed to unmarshal story YAML" file=docs/product/stories/96\.9-document-summary\.yaml
        - stdout matches failed to parse story YAML from docs/product/stories/96\.9-document-summary\.yaml
        - stdout matches mapping values are not allowed in this context
        - stdout matches level=ERROR msg="Refusing to start" command="us refine"
        - but: no file outside "tmp" changed
        - and: the file "docs/product/stories/96.9-document-summary.yaml" is unchanged
        - and: the engine dispatched no AI turns
  E2E-044:
    description: '`us refine` on a story that was never created must be refused, and the refusal must tell the user which command makes the file'
    service: bdd-cli
    path: tests/bdd-cli/us_refine_test.go
    user_stories: []
    merged_steps:
      given:
        - the "us-refine-missing-story" project tree
      when:
        - the Product Owner runs "us refine 96.9"
      then:
        - the command exits with code 1
        - stdout matches US REFINE — Story 96\.9
        - stdout matches level=ERROR msg="No story file found" story_number=96\.9 pattern=docs/product/stories/96\.9-\*\.yaml
        - stdout matches no story file found for story 96\.9 in docs/product/stories
        - 'stdout matches \(expected format: 96\.9-<slug>\.yaml\)'
        - stdout matches level=ERROR msg="Refusing to start" command="us refine"
        - but: no file outside "tmp" changed
        - and: the file "docs/product/stories/96.1-document-summary-refined.yaml" is unchanged
        - and: no file matching "docs/product/stories/96.9-*.yaml" is created
        - and: the engine dispatched no AI turns
  E2E-045:
    description: A story whose acceptance criteria are all error scenarios must fail the happy-path check and pass the rest
    service: bdd-cli
    path: tests/bdd-cli/us_refine_test.go
    user_stories: []
    timeout: 25m
    merged_steps:
      given:
        - the "us-refine-no-happy-path" project tree
      when:
        - the Product Owner runs "us refine 96.4"
      then:
        - the command exits with code 0
        - stdout matches Validation failed. Use --fix flag to enter interactive fix mode.
        - stdout matches REFINE COMPLETE
        - but: no file outside "tmp" changed
        - and: the file "docs/product/stories/96.4-summary-error-messages.yaml" is unchanged
        - and: stdout does not match ALL CHECKS PASSED!
        - and: 'stdout does not match Story saved to:'
  E2E-046:
    description: A story with implementation-level and subjective acceptance criteria must report each failure and leave the story unchanged
    service: bdd-cli
    path: tests/bdd-cli/us_refine_test.go
    user_stories: []
    timeout: 25m
    merged_steps:
      given:
        - the "us-refine-validation-fails" project tree
      when:
        - the Product Owner runs "us refine 96.3"
      then:
        - the command exits with code 0
        - stdout matches Validation failed. Use --fix flag to enter interactive fix mode.
        - stdout matches REFINE COMPLETE
        - but: no file outside "tmp" changed
        - and: the file "docs/product/stories/96.3-summary-service-internals.yaml" is unchanged
        - and: stdout does not match ALL CHECKS PASSED!
        - and: 'stdout does not match Story saved to:'
  E2E-291:
    description: A registry scenario that satisfies every walked prompt must pass the walk and report success
    service: bdd-cli
    path: tests/bdd-cli/scen_check_test.go
    user_stories: []
    timeout: 20m
    merged_steps:
      given:
        - the "scen-check-happy-path" project tree
      when:
        - the System Architect runs "scen check"
      then:
        - the command exits with code 0
        - stdout matches SCEN CHECK
        - and: stdout matches ALL CHECKS PASSED!
        - and: stdout matches SCEN CHECK COMPLETE
        - and: the engine dispatched 8 AI turns
        - and: no file outside "tmp" changed

  E2E-292:
    description: A registry scenario that fails a walked prompt must be reported as failed and still exit 0, because the command is advisory
    service: bdd-cli
    path: tests/bdd-cli/scen_check_test.go
    user_stories: []
    timeout: 20m
    merged_steps:
      given:
        - the "scen-check-validation-fails" project tree
      when:
        - the System Architect runs "scen check"
      then:
        - the command exits with code 0
        - stdout matches SCEN CHECK
        - and: stdout matches Validation failed
        - but: stdout does not match ALL CHECKS PASSED
        - and: the engine dispatched 8 AI turns
        - and: no file outside "tmp" changed

  E2E-293:
    description: A --fix run must be refused before the walk, because the scen-check checklist ships no fix template for any prompt
    service: bdd-cli
    path: tests/bdd-cli/scen_check_test.go
    user_stories: []
    merged_steps:
      given:
        - the "scen-check-fix-refused" project tree
      when:
        - the System Architect runs "scen check --fix"
      then:
        - the command exits with code 1
        - and: stdout matches level=ERROR msg="Refusing to start" command="scen check"
        - but: stdout does not match SCEN CHECK
        - and: no file outside "tmp" changed
        - and: the engine dispatched no AI turns

  E2E-294:
    description: An id naming no scenario in the registry must be refused before the first prompt, never walked as an empty selection
    service: bdd-cli
    path: tests/bdd-cli/scen_check_test.go
    user_stories: []
    merged_steps:
      given:
        - the "scen-check-unknown-id" project tree
      when:
        - the System Architect runs "scen check E2E-999"
      then:
        - the command exits with code 1
        - stdout matches SCEN CHECK
        - and: stdout matches level=ERROR msg="Refusing to start" command="scen check"
        - and: no file outside "tmp" changed
        - and: the engine dispatched no AI turns

  E2E-295:
    description: A registry that parses but holds no scenarios must be refused, so the walk never reports success over nothing
    service: bdd-cli
    path: tests/bdd-cli/scen_check_test.go
    user_stories: []
    merged_steps:
      given:
        - the "scen-check-empty-registry" project tree
      when:
        - the System Architect runs "scen check"
      then:
        - the command exits with code 1
        - stdout matches SCEN CHECK
        - and: stdout matches level=ERROR msg="Refusing to start" command="scen check"
        - and: no file outside "tmp" changed
        - and: the engine dispatched no AI turns

  E2E-296:
    description: Ids given on the command line must narrow the walk to exactly those scenarios, in ascending id order whatever order they were typed
    service: bdd-cli
    path: tests/bdd-cli/scen_check_test.go
    user_stories: []
    timeout: 20m
    merged_steps:
      given:
        - the "scen-check-id-filter" project tree
        - and: the "scen-check" checklist is narrowed to its "whether this scenario's description follows the rule-based format" prompt
      when:
        - the System Architect runs "scen check E2E-002 E2E-001"
      then:
        - the command exits with code 0
        - stdout matches SCEN CHECK
        - and: stdout matches ALL CHECKS PASSED!
        - and: the engine dispatched 2 AI turns
        - but: stdout does not match E2E-003
        - and: no file outside "tmp" changed

  E2E-297:
    description: A checklist that parses but holds no prompts must be refused before the walk, never converge on an item nothing checked
    service: bdd-cli
    path: tests/bdd-cli/build_tests_test.go
    user_stories: []
    merged_steps:
      given:
        - the "build-tests-empty-checklist" project tree
      when:
        - the System Architect runs "build tests"
      then:
        - the command exits with code 1
        - stdout matches level=ERROR msg="Refusing to start" command="build tests"
        - but: stdout does not match BUILD TESTS
        - and: no file outside "tmp" changed
        - and: the engine dispatched no AI turns

  E2E-298:
    description: A host that declares no coverage command must still walk only the scenarios with an unbound step, because the engine resolves step coverage itself
    service: bdd-cli
    path: tests/bdd-cli/build_tests_test.go
    user_stories: []
    merged_steps:
      given:
        - the "build-tests-coverage-in-process" project tree
      when:
        - the System Architect runs "build tests --fix"
      then:
        - the command exits with code 0
        - and: stdout matches 2 of 2 scenario\(s\) already bind every step; walking 0\.
        - and: the engine dispatched no AI turns
        - and: the engine spawned no test runners
        - and: the file "tests/mcp/mcp_test.go" is created
        - but: no file outside "tmp" and "tests" changed

  E2E-299:
    description: A step pattern the engine cannot resolve to a constant must be refused by name, never skipped, because a skipped pattern reports a bound step as unbound
    service: bdd-cli
    path: tests/bdd-cli/build_tests_test.go
    user_stories: []
    merged_steps:
      given:
        - the "build-tests-coverage-computed-pattern" project tree
      when:
        - the System Architect runs "build tests"
      then:
        - the command exits with code 1
        - stdout matches level=ERROR msg="Refusing to start" command="build tests"
        - and: stdout matches step pattern is not a constant expression
        - and: stdout matches tests/mcp/steps/steps\.go
        - but: no file outside "tmp" changed
        - and: the engine dispatched no AI turns

  E2E-300:
    description: A step matched by two definitions must be refused rather than reported as a gap, because which one runs depends on registration order and a third would paper over it
    service: bdd-cli
    path: tests/bdd-cli/build_tests_test.go
    user_stories: []
    merged_steps:
      given:
        - the "build-tests-coverage-ambiguous-step" project tree
      when:
        - the System Architect runs "build tests"
      then:
        - the command exits with code 1
        - stdout matches level=ERROR msg="Refusing to start" command="build tests"
        - and: stdout matches step matches more than one definition
        - but: no file outside "tmp" changed
        - and: the engine dispatched no AI turns
