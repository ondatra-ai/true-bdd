### SYSTEM INSTRUCTIONS (authoritative — follow exactly)

You are a Registry Merge Editor applying fixes to the requirements
registry (the scratch copy of `docs/scenarios.yaml` for this run).

**Your Task:**
1. Read the current state of the scratch registry file.
2. Read the fix prompt instructions.
3. Apply the changes EXACTLY as described — using the Edit tool on the
   scratch registry file path. Do NOT touch the canonical
   `docs/scenarios.yaml`.
4. Emit a short YAML confirmation block.

**Tool Usage:**
- Use Read to inspect the scratch file before editing.
- Use Edit to mutate the scratch file in place.
- Do not write to any other path.

**Output Requirements:**
- After your Edit calls succeed, output a YAML confirmation inside
  FILE_START/FILE_END markers:
  - `applied: true` (or `false` on failure)
  - `target: "<scratch path>"` (QUOTED)
  - `summary: "<one short line describing what changed>"`
- Preserve all other registry entries — only mutate what the fix prompt
  specifies.

**Output Format:**
```
=== FILE_START: tmp/2026-08-17-19-39-44-33520/apply-95.1-001-iter1-result.yaml ===
applied: true
target: "<scratch path>"
summary: "<one-line summary>"
=== FILE_END: tmp/2026-08-17-19-39-44-33520/apply-95.1-001-iter1-result.yaml ===
```

**CRITICAL:**
- Apply changes EXACTLY as described in the fix prompt.
- Do NOT add, remove, or modify entries beyond what the fix prompt
  specifies.
- Edit the scratch file directly during this turn — the confirmation
  block is informational only.


### TASK

## Subject — Acceptance Criterion

**Story ID:** 95.1
**Story Path:** docs/product/stories/95.1-duplicate-collapse.yaml
**AC ID:** AC-1
**Lineage Scenario ID:** 95.1-001
**Description:** The MCP server must respond with a 200 status and a JSON-RPC envelope when a Claude User posts a valid JSON-RPC initialize request to the /mcp endpoint.
**Scratch Registry Path:** tmp/2026-08-17-19-39-44-33520/scenarios.yaml

### Steps
Given:
  - the MCP server is running on its configured port
  - the server accepts JSON-RPC over HTTP POST at /mcp
When:
  - the Claude User posts a valid JSON-RPC initialize request to /mcp
Then:
  - the server returns HTTP 200
  - and the response body is a well-formed JSON-RPC envelope referencing the request id


---

## Fix Prompt to Apply

# Fix Prompt for AC 95.1-001: The MCP server must respond with a 200 status and a JSON-RPC envelope when a Claude User posts a valid JSON-RPC initialize request to the /mcp endpoint.

## Target
Apply the change to: `tmp/2026-08-17-19-39-44-33520/scenarios.yaml`

## Diagnosis
The `us-apply/merge` check failed because two entries reference the subject's lineage scenario id `95.1-001`, for the same story and the same service (`mcp-service`), describing the same observable behavior:

- match: E2E-900 — "MCP /mcp endpoint responds 200 to a valid initialize POST"
- match: E2E-901 — "Valid initialize POST to /mcp returns HTTP 200 with a JSON-RPC envelope"
- Verdict: duplicate (2 entries)

## Resolution plan
- **Winner:** `E2E-900` (prefer the lower-numbered id; both are `E2E-`).
- **Loser:** `E2E-901` (delete).
- **user_stories:** Both entries carry the identical item `(story: docs/product/stories/95.1-duplicate-collapse.yaml, scenario_id: 95.1-001)`. After dedup by `(story, scenario_id)` there is exactly one — no change needed.
- **merged_steps:** Union both entries' steps into the winner, deduplicated and order-preserving:
  - `given`: add `"the server accepts JSON-RPC over HTTP POST at /mcp"` (only in E2E-901) after the shared first step.
  - `when`: identical in both — one step retained.
  - `then`: add `"the response body is a well-formed JSON-RPC envelope referencing the request id"` (only in E2E-901) after the shared first step.
- Do **not** renumber any ids. Leave `E2E-950` untouched.

## Required Changes

### Change #1: Collapse E2E-900 and E2E-901 into a single entry (E2E-900)
**Issue:** E2E-900 and E2E-901 both reference lineage `95.1-001` for the same service — a duplicate merge. The complete behavior (JSON-RPC envelope clause) lives only in E2E-901's steps.

**Action:** Use the Edit tool on the target file with the exact old_string and new_string below. This replaces both entries with the single unioned E2E-900 winner, deleting E2E-901.

**old_string:**
```
  E2E-900:
    description: "MCP /mcp endpoint responds 200 to a valid initialize POST"
    service: "mcp-service"
    last_updated: "2026-07-25"
    user_stories:
      - story: "docs/product/stories/95.1-duplicate-collapse.yaml"
        scenario_id: "95.1-001"
        merge_date: "2026-07-25"
    merged_steps:
      given:
        - "the MCP server is running on its configured port"
      when:
        - "the Claude User posts a valid JSON-RPC initialize request to /mcp"
      then:
        - "the server returns HTTP 200"
  E2E-901:
    description: "Valid initialize POST to /mcp returns HTTP 200 with a JSON-RPC envelope"
    service: "mcp-service"
    last_updated: "2026-07-25"
    user_stories:
      - story: "docs/product/stories/95.1-duplicate-collapse.yaml"
        scenario_id: "95.1-001"
        merge_date: "2026-07-25"
    merged_steps:
      given:
        - "the MCP server is running on its configured port"
        - "the server accepts JSON-RPC over HTTP POST at /mcp"
      when:
        - "the Claude User posts a valid JSON-RPC initialize request to /mcp"
      then:
        - "the server returns HTTP 200"
        - "the response body is a well-formed JSON-RPC envelope referencing the request id"
```
**new_string:**
```
  E2E-900:
    description: "MCP /mcp endpoint responds 200 to a valid initialize POST"
    service: "mcp-service"
    last_updated: "2026-07-25"
    user_stories:
      - story: "docs/product/stories/95.1-duplicate-collapse.yaml"
        scenario_id: "95.1-001"
        merge_date: "2026-07-25"
    merged_steps:
      given:
        - "the MCP server is running on its configured port"
        - "the server accepts JSON-RPC over HTTP POST at /mcp"
      when:
        - "the Claude User posts a valid JSON-RPC initialize request to /mcp"
      then:
        - "the server returns HTTP 200"
        - "the response body is a well-formed JSON-RPC envelope referencing the request id"
```

## Post-fix expectation
After this edit, exactly one entry (`E2E-900`) references lineage `95.1-001`, its `merged_steps` cover the full Given/When/Then of the AC, `E2E-901` is gone, and `E2E-950` is unchanged. Re-running `us-apply/merge` should report: `Verdict: unique`.

---

## Instructions

1. Use the Read tool to load `tmp/2026-08-17-19-39-44-33520/scenarios.yaml` and
   inspect its current state.
2. Apply the changes described in the "Fix Prompt to Apply" section
   above by invoking the Edit tool on
   `tmp/2026-08-17-19-39-44-33520/scenarios.yaml`. Do NOT touch any other path.
3. After the edits succeed, output the confirmation block below.

=== FILE_START: tmp/2026-08-17-19-39-44-33520/apply-95.1-001-iter1-result.yaml ===
applied: true
target: "tmp/2026-08-17-19-39-44-33520/scenarios.yaml"
summary: "<one-line summary of what changed>"
=== FILE_END: tmp/2026-08-17-19-39-44-33520/apply-95.1-001-iter1-result.yaml ===
