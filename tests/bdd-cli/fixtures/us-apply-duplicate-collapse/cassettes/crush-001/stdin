### SYSTEM INSTRUCTIONS (authoritative — follow exactly)

You are a Registry Merge Editor applying fixes to the requirements
registry (the scratch copy of `docs/scenarios.yaml` for this run).

**Your Task:**
1. Read the current state of the scratch registry file.
2. Read the fix prompt instructions.
3. Apply the changes EXACTLY as described — using the Edit tool on the
   scratch registry file path. Do NOT touch the canonical
   `docs/scenarios.yaml`.
4. Emit a short YAML confirmation block.

**Tool Usage:**
- Use Read to inspect the scratch file before editing.
- Use Edit to mutate the scratch file in place.
- Do not write to any other path.

**Output Requirements:**
- After your Edit calls succeed, output a YAML confirmation inside
  FILE_START/FILE_END markers:
  - `applied: true` (or `false` on failure)
  - `target: "<scratch path>"` (QUOTED)
  - `summary: "<one short line describing what changed>"`
- Preserve all other registry entries — only mutate what the fix prompt
  specifies.

**Output Format:**
```
=== FILE_START: tmp/{{RUN_DIR}}/apply-95.1-001-iter1-result.yaml ===
applied: true
target: "<scratch path>"
summary: "<one-line summary>"
=== FILE_END: tmp/{{RUN_DIR}}/apply-95.1-001-iter1-result.yaml ===
```

**CRITICAL:**
- Apply changes EXACTLY as described in the fix prompt.
- Do NOT add, remove, or modify entries beyond what the fix prompt
  specifies.
- Edit the scratch file directly during this turn — the confirmation
  block is informational only.


### TASK

## Subject — Acceptance Criterion

**Story ID:** 95.1
**Story Path:** docs/product/stories/95.1-duplicate-collapse.yaml
**AC ID:** AC-1
**Lineage Scenario ID:** 95.1-001
**Description:** The MCP server must respond with a 200 status and a JSON-RPC envelope when a Claude User posts a valid JSON-RPC initialize request to the /mcp endpoint.
**Scratch Registry Path:** tmp/{{RUN_DIR}}/scenarios.yaml

### Steps
Given:
  - the MCP server is running on its configured port
  - the server accepts JSON-RPC over HTTP POST at /mcp
When:
  - the Claude User posts a valid JSON-RPC initialize request to /mcp
Then:
  - the server returns HTTP 200
  - and the response body is a well-formed JSON-RPC envelope referencing the request id


---

## Fix Prompt to Apply

# Fix Prompt for AC 95.1-001: The MCP server must respond with a 200 status and a JSON-RPC envelope when a Claude User posts a valid JSON-RPC initialize request to the /mcp endpoint.

## Target
Apply the change to: `tmp/{{RUN_DIR}}/scenarios.yaml`

## Situation
Two entries reference lineage scenario id `95.1-001` for story `docs/product/stories/95.1-duplicate-collapse.yaml`:

- `E2E-900` — "MCP /mcp endpoint responds 200 to a valid initialize POST"
- `E2E-901` — "Valid initialize POST to /mcp returns HTTP 200 with a JSON-RPC envelope"

Winner: **E2E-900** (lower-numbered id; both are `E2E-`, so the `INT-` preference does not apply).
Loser: **E2E-901** (deleted).

`user_stories[]`: both entries carry the identical tuple `(docs/product/stories/95.1-duplicate-collapse.yaml, 95.1-001)`, so moving the loser's item into the winner and deduplicating by `(story, scenario_id)` leaves E2E-900's `user_stories:` block exactly as it is — no edit is needed there.

`merged_steps`: E2E-901 carries two steps E2E-900 lacks (the `given:` about JSON-RPC over HTTP POST at /mcp, and the `then:` about the JSON-RPC envelope). Both are unioned into E2E-900.

Do NOT renumber any id, do NOT touch `E2E-950`, and do NOT change E2E-900's `description:`, `service:`, `last_updated:`, or the file's header comments.

## Required Changes

### Change #1: Union E2E-901's merged_steps into E2E-900 and delete E2E-901
**Issue:** `E2E-900` and `E2E-901` both reference lineage `95.1-001`, so the registry holds duplicate entries for this AC. The `us-apply/merge` duplicate check fails with 2 matching entries.
**Action:** Use the Edit tool on the target file with the exact old_string and new_string below. This single edit performs both the `merged_steps` union (into the winner E2E-900) and the deletion of the losing entry E2E-901. The trailing `  E2E-950:` line is included only as an anchor so the match is unique — keep it verbatim in the replacement.
**old_string:**
```
    merged_steps:
      given:
        - "the MCP server is running on its configured port"
      when:
        - "the Claude User posts a valid JSON-RPC initialize request to /mcp"
      then:
        - "the server returns HTTP 200"
  E2E-901:
    description: "Valid initialize POST to /mcp returns HTTP 200 with a JSON-RPC envelope"
    service: "mcp-service"
    last_updated: "2026-07-25"
    user_stories:
      - story: "docs/product/stories/95.1-duplicate-collapse.yaml"
        scenario_id: "95.1-001"
        merge_date: "2026-07-25"
    merged_steps:
      given:
        - "the MCP server is running on its configured port"
        - "the server accepts JSON-RPC over HTTP POST at /mcp"
      when:
        - "the Claude User posts a valid JSON-RPC initialize request to /mcp"
      then:
        - "the server returns HTTP 200"
        - "the response body is a well-formed JSON-RPC envelope referencing the request id"
  E2E-950:
```
**new_string:**
```
    merged_steps:
      given:
        - "the MCP server is running on its configured port"
        - "the server accepts JSON-RPC over HTTP POST at /mcp"
      when:
        - "the Claude User posts a valid JSON-RPC initialize request to /mcp"
      then:
        - "the server returns HTTP 200"
        - "the response body is a well-formed JSON-RPC envelope referencing the request id"
  E2E-950:
```

## Expected Result
After the edit, `scenarios:` holds exactly two entries — `E2E-900` and `E2E-950` — in that order:

- `E2E-900` keeps its original `description:`, `service: "mcp-service"`, `last_updated: "2026-07-25"` and its single `user_stories[]` item for `95.1-001`, and now carries the unioned `merged_steps` (2 given, 1 when, 2 then).
- `E2E-901` is gone.
- `E2E-950` is byte-for-byte unchanged.

Exactly one entry references lineage `95.1-001`, so the `merge` section's "present?" prompt still passes and the "duplicate?" prompt now passes as `Verdict: unique`.

---

## Instructions

1. Use the Read tool to load `tmp/{{RUN_DIR}}/scenarios.yaml` and
   inspect its current state.
2. Apply the changes described in the "Fix Prompt to Apply" section
   above by invoking the Edit tool on
   `tmp/{{RUN_DIR}}/scenarios.yaml`. Do NOT touch any other path.
3. After the edits succeed, output the confirmation block below.

=== FILE_START: tmp/{{RUN_DIR}}/apply-95.1-001-iter1-result.yaml ===
applied: true
target: "tmp/{{RUN_DIR}}/scenarios.yaml"
summary: "<one-line summary of what changed>"
=== FILE_END: tmp/{{RUN_DIR}}/apply-95.1-001-iter1-result.yaml ===
