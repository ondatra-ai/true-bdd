### SYSTEM INSTRUCTIONS (authoritative — follow exactly)

You are a Technical Scrum Master applying fixes to a user story.

**Your Task:**
1. Read the current story and the fix prompt instructions.
2. Apply the changes EXACTLY as specified in the fix prompt — they may
   target any field of the story (`title`, `as_a`, `i_want`, `so_that`,
   `status`, `acceptance_criteria`, …), not only acceptance criteria.
3. Output the COMPLETE updated story body in YAML form.

**Output Requirements:**
- Output every field of the current story, with the fix prompt's
  changes applied. Fields not mentioned by the fix prompt MUST be
  preserved verbatim — do not invent, drop, or reorder unrelated
  fields.
- The `acceptance_criteria` array MUST always be present, even when
  the fix only touches a top-level field; preserve all ACs unless the
  fix prompt explicitly modifies them.
- Each acceptance criterion must have an `id`, `description`, and
  `steps` field. The `description` is a one-line rule-based statement
  using must/should (NO Gherkin in `description`). The `steps` field
  holds the Gherkin scenario as a structured given/when/then list.
- Wrap the output with FILE_START/FILE_END markers.

**Output Format:**
```
=== FILE_START: tmp/2026-08-15-16-57-34-7044/apply-96.5-iter1-result.yaml ===
id: "<story id>"
title: "<story title>"
as_a: "<role>"
i_want: "<desire>"
so_that: "<benefit>"
status: "<status>"
acceptance_criteria:
  - id: "AC-1"
    description: "One-line rule-based statement with must/should"
    steps:
      - given:
          - "precondition"
      - when:
          - "action"
      - then:
          - "outcome"
          - and: "additional outcome"
  - id: "AC-2"
    description: "Another one-line rule-based statement"
    steps:
      - given:
          - "precondition"
      - when:
          - "action"
      - then:
          - "outcome"
=== FILE_END: tmp/2026-08-15-16-57-34-7044/apply-96.5-iter1-result.yaml ===
```

**CRITICAL:**
- Apply changes EXACTLY as described in the fix prompt.
- Output the WHOLE story body — top-level fields AND
  `acceptance_criteria`. Never output just one section.
- Do NOT add, remove, or rename fields beyond what the fix prompt
  specifies.
- Preserve the exact wording from the fix prompt's "After" sections.
- The `description` must NEVER contain Given/When/Then — those belong
  in `steps`.


### TASK

## Current Story

**ID:** 96.5
**Title:** Document Summary On Demand
**As a:** Claude User
**I want:** Claude to give me a short summary of a shared Google Doc when I ask for one
**So that:** I can decide whether the document needs my attention without reading it in full
**Status:** PLANNED

### Current Acceptance Criteria

**AC-1:**
Claude must display a summary of 150 words or fewer in the conversation when the Claude User asks for a summary of a shared Google Doc.

**AC-2:**
Claude must display the message 'This document is not shared with your account' when the Claude User asks for a summary of a document they cannot access.

**AC-3:**
Claude must display the message 'The summary timed out - please try again' when no summary is produced within 30 seconds.

**AC-4:**
Claude must state that only the first 10000 words were summarized when the document contains more than 10000 words.

**AC-5:**
Claude must list every section heading of a shared Google Doc in the conversation when the Claude User asks for the document outline.


---

## Fix Prompt to Apply

# Fix Prompt for Story 96.5: Document Summary On Demand

## Instructions
Apply the following changes to the acceptance criteria for this story.

## Original Acceptance Criteria
1. AC-1: Claude must display a summary of 150 words or fewer in the conversation when the Claude User asks for a summary of a shared Google Doc.
2. AC-2: Claude must display the message 'This document is not shared with your account' when the Claude User asks for a summary of a document they cannot access.
3. AC-3: Claude must display the message 'The summary timed out - please try again' when no summary is produced within 30 seconds.
4. AC-4: Claude must state that only the first 10000 words were summarized when the document contains more than 10000 words.
5. AC-5: Claude must list every section heading of a shared Google Doc in the conversation when the Claude User asks for the document outline.

## Required Changes

The failed check is `us-refine/acceptance_criteria`: every AC's `steps` field must be present and contain at least one Given, one When, and one Then step. The AC `description` texts are already valid rule-based statements (`must` + observable outcome) and are left unchanged. The fix adds the missing `steps` for AC-2 and normalizes Given/When/Then for every AC so all pass.

Per-AC expectation resolution:
- AC-1: ok
- AC-2: missing steps field — added below (primary reported failure)
- AC-3: ok
- AC-4: ok
- AC-5: ok

### Change #1: AC-2
**Before:** Claude must display the message 'This document is not shared with your account' when the Claude User asks for a summary of a document they cannot access.
**Issue:** `steps` field is missing — no Given/When/Then, so the AC has no executable specification.
**After (description):** Claude must display the message 'This document is not shared with your account' when the Claude User asks for a summary of a document they cannot access.
**After (steps):**
```yaml
- given:
    - "a Google Doc is not shared with the Claude User's account"
- when:
    - "the Claude User asks Claude to summarize the document"
- then:
    - "Claude displays the message 'This document is not shared with your account'"
```

### Change #2: AC-1 (normalize steps)
**Before:** Claude must display a summary of 150 words or fewer in the conversation when the Claude User asks for a summary of a shared Google Doc.
**Issue:** ensure a complete Given/When/Then `steps` field is present.
**After (description):** Claude must display a summary of 150 words or fewer in the conversation when the Claude User asks for a summary of a shared Google Doc.
**After (steps):**
```yaml
- given:
    - "a Google Doc is shared with the Claude User's account"
- when:
    - "the Claude User asks Claude to summarize the shared Google Doc"
- then:
    - "Claude displays a summary of 150 words or fewer in the conversation"
```

### Change #3: AC-3 (normalize steps)
**Before:** Claude must display the message 'The summary timed out - please try again' when no summary is produced within 30 seconds.
**Issue:** ensure a complete Given/When/Then `steps` field is present.
**After (description):** Claude must display the message 'The summary timed out - please try again' when no summary is produced within 30 seconds.
**After (steps):**
```yaml
- given:
    - "the Claude User has asked Claude to summarize a shared Google Doc"
- when:
    - "no summary is produced within 30 seconds"
- then:
    - "Claude displays the message 'The summary timed out - please try again'"
```

### Change #4: AC-4 (normalize steps)
**Before:** Claude must state that only the first 10000 words were summarized when the document contains more than 10000 words.
**Issue:** ensure a complete Given/When/Then `steps` field is present.
**After (description):** Claude must state that only the first 10000 words were summarized when the document contains more than 10000 words.
**After (steps):**
```yaml
- given:
    - "a shared Google Doc contains more than 10000 words"
- when:
    - "the Claude User asks Claude to summarize the document"
- then:
    - "Claude states that only the first 10000 words were summarized"
```

### Change #5: AC-5 (normalize steps)
**Before:** Claude must list every section heading of a shared Google Doc in the conversation when the Claude User asks for the document outline.
**Issue:** ensure a complete Given/When/Then `steps` field is present.
**After (description):** Claude must list every section heading of a shared Google Doc in the conversation when the Claude User asks for the document outline.
**After (steps):**
```yaml
- given:
    - "a Google Doc with section headings is shared with the Claude User's account"
- when:
    - "the Claude User asks Claude for the document outline"
- then:
    - "Claude lists every section heading of the shared Google Doc in the conversation"
```

## Complete Fixed Acceptance Criteria

```yaml
- ac_id: AC-1
  description: "Claude must display a summary of 150 words or fewer in the conversation when the Claude User asks for a summary of a shared Google Doc."
  steps:
    - given:
        - "a Google Doc is shared with the Claude User's account"
    - when:
        - "the Claude User asks Claude to summarize the shared Google Doc"
    - then:
        - "Claude displays a summary of 150 words or fewer in the conversation"

- ac_id: AC-2
  description: "Claude must display the message 'This document is not shared with your account' when the Claude User asks for a summary of a document they cannot access."
  steps:
    - given:
        - "a Google Doc is not shared with the Claude User's account"
    - when:
        - "the Claude User asks Claude to summarize the document"
    - then:
        - "Claude displays the message 'This document is not shared with your account'"

- ac_id: AC-3
  description: "Claude must display the message 'The summary timed out - please try again' when no summary is produced within 30 seconds."
  steps:
    - given:
        - "the Claude User has asked Claude to summarize a shared Google Doc"
    - when:
        - "no summary is produced within 30 seconds"
    - then:
        - "Claude displays the message 'The summary timed out - please try again'"

- ac_id: AC-4
  description: "Claude must state that only the first 10000 words were summarized when the document contains more than 10000 words."
  steps:
    - given:
        - "a shared Google Doc contains more than 10000 words"
    - when:
        - "the Claude User asks Claude to summarize the document"
    - then:
        - "Claude states that only the first 10000 words were summarized"

- ac_id: AC-5
  description: "Claude must list every section heading of a shared Google Doc in the conversation when the Claude User asks for the document outline."
  steps:
    - given:
        - "a Google Doc with section headings is shared with the Claude User's account"
    - when:
        - "the Claude User asks Claude for the document outline"
    - then:
        - "Claude lists every section heading of the shared Google Doc in the conversation"
```

---

## Instructions

Apply the changes described in the "Fix Prompt to Apply" section above.
The fix may target any field of the story — top-level (`title`,
`as_a`, `i_want`, `so_that`, `status`) or `acceptance_criteria` —
so output the COMPLETE updated story body. Preserve every field that
the fix prompt does not touch.

Output the complete updated story body using FILE_START/FILE_END
markers:

=== FILE_START: tmp/2026-08-15-16-57-34-7044/apply-96.5-iter1-result.yaml ===
(Complete story body YAML here — every top-level field plus
`acceptance_criteria`)
=== FILE_END: tmp/2026-08-15-16-57-34-7044/apply-96.5-iter1-result.yaml ===
