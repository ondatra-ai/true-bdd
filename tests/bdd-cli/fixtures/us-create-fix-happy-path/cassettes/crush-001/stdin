### SYSTEM INSTRUCTIONS (authoritative — follow exactly)

You are a Technical Scrum Master applying fixes to a user story.

**Your Task:**
1. Read the current story and the fix prompt instructions.
2. Apply the changes EXACTLY as specified in the fix prompt — they may
   target any field of the story (`title`, `as_a`, `i_want`, `so_that`,
   `status`, `acceptance_criteria`, …), not only acceptance criteria.
3. Output the COMPLETE updated story body in YAML form.

**Output Requirements:**
- Output every field of the current story, with the fix prompt's
  changes applied. Fields not mentioned by the fix prompt MUST be
  preserved verbatim — do not invent, drop, or reorder unrelated
  fields.
- The `acceptance_criteria` array MUST always be present, even when
  the fix only touches a top-level field; preserve all ACs unless the
  fix prompt explicitly modifies them.
- Each acceptance criterion must have an `id`, `description`, and
  `steps` field. The `description` is a one-line rule-based statement
  using must/should (NO Gherkin in `description`). The `steps` field
  holds the Gherkin scenario as a structured given/when/then list.
- Wrap the output with FILE_START/FILE_END markers.

**Output Format:**
```
=== FILE_START: tmp/2026-08-15-18-06-17-76507/apply-99.1-iter1-result.yaml ===
id: "<story id>"
title: "<story title>"
as_a: "<role>"
i_want: "<desire>"
so_that: "<benefit>"
status: "<status>"
acceptance_criteria:
  - id: "AC-1"
    description: "One-line rule-based statement with must/should"
    steps:
      - given:
          - "precondition"
      - when:
          - "action"
      - then:
          - "outcome"
          - and: "additional outcome"
  - id: "AC-2"
    description: "Another one-line rule-based statement"
    steps:
      - given:
          - "precondition"
      - when:
          - "action"
      - then:
          - "outcome"
=== FILE_END: tmp/2026-08-15-18-06-17-76507/apply-99.1-iter1-result.yaml ===
```

**CRITICAL:**
- Apply changes EXACTLY as described in the fix prompt.
- Output the WHOLE story body — top-level fields AND
  `acceptance_criteria`. Never output just one section.
- Do NOT add, remove, or rename fields beyond what the fix prompt
  specifies.
- Preserve the exact wording from the fix prompt's "After" sections.
- The `description` must NEVER contain Given/When/Then — those belong
  in `steps`.


### TASK

## Current Story

**ID:** 99.1
**Title:** Document Summary On Demand
**As a:** Claude User
**I want:** Claude to give me a short summary of a Google Doc when I ask
**So that:** I do not have to download the document or copy paste large sections of it into a separate REST endpoint just to get a quick gist
**Status:** PLANNED

### Current Acceptance Criteria

**AC-1:**
When I ask Claude to summarize a shared Google Doc, I receive a paragraph-length summary in the conversation.

**AC-2:**
When I ask Claude to summarize a document I have not shared, I receive a clear message explaining I need to share it first.

**AC-3:**
When the document is too long to summarize confidently, I receive a message telling me Claude only read part of it.


---

## Fix Prompt to Apply

# Fix Prompt for Story 99.1: Document Summary On Demand

## Instructions
Apply the following changes to this story. The failed check `us-create/why`
faults the story's **"so that"** clause, not the acceptance criteria, so the
only edit is to that clause. Every acceptance criterion is reproduced exactly
as it stands.

## Validation Failure Recap
- **Check:** `us-create/why` — count internal implementation terms in the "so that" clause (pass = 0).
- **Implementation terms in 'so that': 1**
  - REST: "into a separate **REST** endpoint just to get a quick gist"
  - (the phrase "REST endpoint" is a single implementation concept — our API surface, not a product the Claude User interacts with)

## Original Story Statement
**As a** Claude User
**I want** Claude to give me a short summary of a Google Doc when I ask
**So that** I do not have to download the document or copy paste large sections of it into a separate REST endpoint just to get a quick gist

## Required Changes

### Change #1: "so that" clause
**Before:** So that I do not have to download the document or copy paste large sections of it into a separate REST endpoint just to get a quick gist
**Issue:** "REST endpoint" is an internal implementation term (our API surface). A non-technical Claude User does not interact with a REST endpoint — it describes HOW the system is built, not WHY the user wants the feature. The check requires zero implementation terms in the "so that" clause.
**After:** So that I do not have to download the document or copy paste large sections of it into a separate tool just to get a quick gist

## Original Acceptance Criteria (unchanged by this fix)
1. AC-1: When I ask Claude to summarize a shared Google Doc, I receive a paragraph-length summary in the conversation. [no steps]
2. AC-2: When I ask Claude to summarize a document I have not shared, I receive a clear message explaining I need to share it first. [no steps]
3. AC-3: When the document is too long to summarize confidently, I receive a message telling me Claude only read part of it. [no steps]

## Complete Fixed Story

```yaml
as_a: "Claude User"
i_want: "Claude to give me a short summary of a Google Doc when I ask"
so_that: "I do not have to download the document or copy paste large sections of it into a separate tool just to get a quick gist"

acceptance_criteria:
  - ac_id: AC-1
    description: "When I ask Claude to summarize a shared Google Doc, I receive a paragraph-length summary in the conversation."

  - ac_id: AC-2
    description: "When I ask Claude to summarize a document I have not shared, I receive a clear message explaining I need to share it first."

  - ac_id: AC-3
    description: "When the document is too long to summarize confidently, I receive a message telling me Claude only read part of it."
```

---

## Instructions

Apply the changes described in the "Fix Prompt to Apply" section above.
The fix may target any field of the story — top-level (`title`,
`as_a`, `i_want`, `so_that`, `status`) or `acceptance_criteria` —
so output the COMPLETE updated story body. Preserve every field that
the fix prompt does not touch.

Output the complete updated story body using FILE_START/FILE_END
markers:

=== FILE_START: tmp/2026-08-15-18-06-17-76507/apply-99.1-iter1-result.yaml ===
(Complete story body YAML here — every top-level field plus
`acceptance_criteria`)
=== FILE_END: tmp/2026-08-15-18-06-17-76507/apply-99.1-iter1-result.yaml ===
