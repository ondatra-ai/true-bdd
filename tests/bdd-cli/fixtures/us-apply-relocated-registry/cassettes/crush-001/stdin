### SYSTEM INSTRUCTIONS (authoritative — follow exactly)

You are a Registry Merge Editor applying fixes to the requirements
registry (the scratch copy of `docs/scenarios.yaml` for this run).

**Your Task:**
1. Read the current state of the scratch registry file.
2. Read the fix prompt instructions.
3. Apply the changes EXACTLY as described — using the Edit tool on the
   scratch registry file path. Do NOT touch the canonical
   `docs/scenarios.yaml`.
4. Emit a short YAML confirmation block.

**Tool Usage:**
- Use Read to inspect the scratch file before editing.
- Use Edit to mutate the scratch file in place.
- Do not write to any other path.

**Output Requirements:**
- After your Edit calls succeed, output a YAML confirmation inside
  FILE_START/FILE_END markers:
  - `applied: true` (or `false` on failure)
  - `target: "<scratch path>"` (QUOTED)
  - `summary: "<one short line describing what changed>"`
- Preserve all other registry entries — only mutate what the fix prompt
  specifies.

**Output Format:**
```
=== FILE_START: tmp/2026-08-17-19-43-05-35899/apply-95.2-001-iter1-result.yaml ===
applied: true
target: "<scratch path>"
summary: "<one-line summary>"
=== FILE_END: tmp/2026-08-17-19-43-05-35899/apply-95.2-001-iter1-result.yaml ===
```

**CRITICAL:**
- Apply changes EXACTLY as described in the fix prompt.
- Do NOT add, remove, or modify entries beyond what the fix prompt
  specifies.
- Edit the scratch file directly during this turn — the confirmation
  block is informational only.


### TASK

## Subject — Acceptance Criterion

**Story ID:** 95.2
**Story Path:** docs/product/stories/95.2-relocated-registry.yaml
**AC ID:** AC-1
**Lineage Scenario ID:** 95.2-001
**Description:** The MCP server must respond with a 200 status and a JSON body reporting the service as healthy when a Claude User sends a GET request to the /health endpoint.
**Scratch Registry Path:** tmp/2026-08-17-19-43-05-35899/scenarios.yaml

### Steps
Given:
  - the MCP server is running on its configured port
  - the server exposes a health endpoint at /health
When:
  - the Claude User sends a GET request to /health
Then:
  - the server returns HTTP 200
  - and the response body reports the service status as healthy


---

## Fix Prompt to Apply

# Fix Prompt for AC 95.2-001: The MCP server must respond with a 200 status and a JSON body reporting the service as healthy when a Claude User sends a GET request to the /health endpoint.

## Target
Apply the change to: `tmp/2026-08-17-19-43-05-35899/scenarios.yaml`

## Context
- The `us-apply/merge` check failed: no entry under `scenarios:` has a `user_stories[]` member carrying both the story path `docs/product/stories/95.2-relocated-registry.yaml` and the lineage scenario id `95.2-001`.
- The only existing entry is `E2E-960`. It uses `service`, `last_updated`, `user_stories`, `merged_steps` — and **no `path:` field**. All step strings are double-quoted.
- No architecture doc / suite-path reference is provided, and there is no existing `path:` to reuse, so the new entry mirrors `E2E-960`'s exact shape (no `path:` field) rather than inventing one.
- Id chosen: `E2E-961` — the next free id following the existing `E2E-960` numbering. This is an e2e/integration behaviour (Claude User → MCP server health endpoint), and the template prefers `E2E-NNN` when unsure.
- `service` is set to `""` per the template default, because the subject names no service.
- `given` / `when` / `then` are copied verbatim from the AC's steps. Dates use today (`2026-08-17`).

## Required Changes
### Change #1: Append a new scenario entry for AC 95.2-001
**Issue:** The registry has no scenario whose `user_stories[]` links story `docs/product/stories/95.2-relocated-registry.yaml` with lineage id `95.2-001`, so the "present?" merge check fails.
**Action:** Use the Edit tool on the target file with the exact old_string and new_string below. This appends `E2E-961` after `E2E-960` and leaves `E2E-960` untouched.
**old_string:**
```
      when:
        - "the Claude User opens the settings page"
      then:
        - "the settings page renders the account section"
```
**new_string:**
```
      when:
        - "the Claude User opens the settings page"
      then:
        - "the settings page renders the account section"

  E2E-961:
    description: "The MCP server returns HTTP 200 with a JSON body reporting the service as healthy for a GET request to /health"
    service: ""
    last_updated: "2026-08-17"
    user_stories:
      - story: "docs/product/stories/95.2-relocated-registry.yaml"
        scenario_id: "95.2-001"
        merge_date: "2026-08-17"
    merged_steps:
      given:
        - "the MCP server is running on its configured port"
        - "the server exposes a health endpoint at /health"
      when:
        - "the Claude User sends a GET request to /health"
      then:
        - "the server returns HTTP 200"
        - "and the response body reports the service status as healthy"
```

---

## Instructions

1. Use the Read tool to load `tmp/2026-08-17-19-43-05-35899/scenarios.yaml` and
   inspect its current state.
2. Apply the changes described in the "Fix Prompt to Apply" section
   above by invoking the Edit tool on
   `tmp/2026-08-17-19-43-05-35899/scenarios.yaml`. Do NOT touch any other path.
3. After the edits succeed, output the confirmation block below.

=== FILE_START: tmp/2026-08-17-19-43-05-35899/apply-95.2-001-iter1-result.yaml ===
applied: true
target: "tmp/2026-08-17-19-43-05-35899/scenarios.yaml"
summary: "<one-line summary of what changed>"
=== FILE_END: tmp/2026-08-17-19-43-05-35899/apply-95.2-001-iter1-result.yaml ===
