### SYSTEM INSTRUCTIONS (authoritative — follow exactly)

You are a Registry Merge Editor applying fixes to the requirements
registry (the scratch copy of `docs/scenarios.yaml` for this run).

**Your Task:**
1. Read the current state of the scratch registry file.
2. Read the fix prompt instructions.
3. Apply the changes EXACTLY as described — using the Edit tool on the
   scratch registry file path. Do NOT touch the canonical
   `docs/scenarios.yaml`.
4. Emit a short YAML confirmation block.

**Tool Usage:**
- Use Read to inspect the scratch file before editing.
- Use Edit to mutate the scratch file in place.
- Do not write to any other path.

**Output Requirements:**
- After your Edit calls succeed, output a YAML confirmation inside
  FILE_START/FILE_END markers:
  - `applied: true` (or `false` on failure)
  - `target: "<scratch path>"` (QUOTED)
  - `summary: "<one short line describing what changed>"`
- Preserve all other registry entries — only mutate what the fix prompt
  specifies.

**Output Format:**
```
=== FILE_START: tmp/{{RUN_DIR}}/apply-95.2-001-iter1-result.yaml ===
applied: true
target: "<scratch path>"
summary: "<one-line summary>"
=== FILE_END: tmp/{{RUN_DIR}}/apply-95.2-001-iter1-result.yaml ===
```

**CRITICAL:**
- Apply changes EXACTLY as described in the fix prompt.
- Do NOT add, remove, or modify entries beyond what the fix prompt
  specifies.
- Edit the scratch file directly during this turn — the confirmation
  block is informational only.


### TASK

## Subject — Acceptance Criterion

**Story ID:** 95.2
**Story Path:** docs/product/stories/95.2-relocated-registry.yaml
**AC ID:** AC-1
**Lineage Scenario ID:** 95.2-001
**Description:** The MCP server must respond with a 200 status and a JSON body reporting the service as healthy when a Claude User sends a GET request to the /health endpoint.
**Scratch Registry Path:** tmp/{{RUN_DIR}}/scenarios.yaml

### Steps
Given:
  - the MCP server is running on its configured port
  - the server exposes a health endpoint at /health
When:
  - the Claude User sends a GET request to /health
Then:
  - the server returns HTTP 200
  - and the response body reports the service status as healthy


---

## Fix Prompt to Apply

# Fix Prompt for AC 95.2-001: The MCP server must respond with a 200 status and a JSON body reporting the service as healthy when a Claude User sends a GET request to the /health endpoint.

## Target
Apply the change to: `tmp/{{RUN_DIR}}/scenarios.yaml`

## Required Changes

### Change #1: Add a new scenario entry for AC 95.2-001
**Issue:** The `us-apply/merge` "present?" check fails — no entry under `scenarios:` has a `user_stories[]` item referencing story `docs/product/stories/95.2-relocated-registry.yaml` with lineage id `95.2-001`. The AC must be added as exactly one new scenario, without touching the existing `E2E-960` entry.

**Decisions (do not improvise these):**
- **id:** `E2E-961` — the next free id after the only existing id `E2E-960`; e2e-layer (an HTTP GET against a running server), and the F: template says prefer `E2E-` when unsure.
- **service:** `""` — verbatim per the F: template. Do NOT copy `"web-service"` from E2E-960; that would falsely bind this AC to the web suite.
- **path:** `"tests/mcp/health_test.go"` — the registry has no other scenario of this family to reuse a `path:` from and no `architecture.testing.suites[]` to anchor to, so this is the invented `<suite path>/<family>_test.go` value. Use it as a literal; do not substitute another path.
- **dates:** `"2026-08-31"` for both `last_updated` and `merge_date`.
- **steps:** copied verbatim from the story's `steps.given` / `steps.when` / `steps.then`. Preserve the `then:` modifier-form step exactly as `- and: "..."` (the schema allows the one-key `{and: …}` mapping).
- This is an **append-only** edit — `E2E-960` and `metadata:` stay byte-identical.

**Action:** Use the Edit tool on the target file with the exact old_string and new_string below. The anchor is E2E-960's final two lines (unique in the file); the new block is appended after a single blank line.

**old_string:**
```
      then:
        - "the settings page renders the account section"
```

**new_string:**
```
      then:
        - "the settings page renders the account section"

  E2E-961:
    description: "MCP server returns HTTP 200 and healthy JSON on GET /health"
    service: ""
    path: "tests/mcp/health_test.go"
    last_updated: "2026-08-31"
    user_stories:
      - story: "docs/product/stories/95.2-relocated-registry.yaml"
        scenario_id: "95.2-001"
        merge_date: "2026-08-31"
    merged_steps:
      given:
        - "the MCP server is running on its configured port"
        - "the server exposes a health endpoint at /health"
      when:
        - "the Claude User sends a GET request to /health"
      then:
        - "the server returns HTTP 200"
        - and: "the response body reports the service status as healthy"
```

## Verification after applying
- `scenarios:` now contains an entry (`E2E-961`) whose `user_stories[]` lists `story: docs/product/stories/95.2-relocated-registry.yaml` with `scenario_id: 95.2-001` → `us-apply/merge` "present?" passes.
- Exactly one entry references lineage `95.2-001` → "duplicate?" stays passing.
- `E2E-960` and `metadata:` are unchanged (append-only edit).

---

## Instructions

1. Use the Read tool to load `tmp/{{RUN_DIR}}/scenarios.yaml` and
   inspect its current state.
2. Apply the changes described in the "Fix Prompt to Apply" section
   above by invoking the Edit tool on
   `tmp/{{RUN_DIR}}/scenarios.yaml`. Do NOT touch any other path.
3. After the edits succeed, output the confirmation block below.

=== FILE_START: tmp/{{RUN_DIR}}/apply-95.2-001-iter1-result.yaml ===
applied: true
target: "tmp/{{RUN_DIR}}/scenarios.yaml"
summary: "<one-line summary of what changed>"
=== FILE_END: tmp/{{RUN_DIR}}/apply-95.2-001-iter1-result.yaml ===
