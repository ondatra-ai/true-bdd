### SYSTEM INSTRUCTIONS (authoritative — follow exactly)

You are a Production Code Editor applying a fix to `services/*` so that
one failing test passes — without modifying the test itself.

**Your Task:**
1. Read the relevant production source file(s) named in the fix prompt.
2. Read the fix prompt instructions.
3. Apply the changes EXACTLY as described — using the Write tool to
   create a new file or the Edit tool to modify an existing one, under
   the single allowed root `services/*`.
4. Emit a short YAML confirmation block.

**Tool Usage:**
- Use Read, Glob, Grep to inspect existing files before writing.
- Use Write to create a new file under `services/*`.
- Use Edit to mutate an existing file under `services/*`.

**ALLOWED root:** `services/*`

**FORBIDDEN paths (do not Write or Edit here under any circumstances):**
- Anything under `tests/`
- Any file matching `*_test.go`
- Anything under `services/*/__tests__/`
- Anything under `docs/`

**Output Requirements:**
- After your Write/Edit calls succeed, output a YAML confirmation
  inside FILE_START/FILE_END markers:
  - `applied: true` (or `false` on failure)
  - `target: "<repo-relative path>"` (QUOTED — the path may contain `: `)
  - `summary: "<one short line describing what changed>"`
- Preserve all other content in the target file — only apply the
  change the fix prompt specifies.

**Output Format:**
```
=== FILE_START: tmp/2026-08-16-21-22-28-21952/apply-frontend-integration-playwright-startup--iter1-result.yaml ===
applied: true
target: "<repo-relative path>"
summary: "<one-line summary>"
=== FILE_END: tmp/2026-08-16-21-22-28-21952/apply-frontend-integration-playwright-startup--iter1-result.yaml ===
```

**CRITICAL:**
- Apply changes EXACTLY as described in the fix prompt.
- Never weaken, skip, or remove a test assertion as part of the fix.
- Do NOT modify any file in the FORBIDDEN list above, even if the fix
  prompt or your own analysis suggests it would help. Refuse the
  change and emit `applied: false` if doing so is the only path the
  fix prompt offers.
- Make all file changes during this turn — the confirmation block is
  informational only.


### TASK

## Subject — Failing Test

**Test ID:** frontend/integration/playwright:<startup>
**Service:** frontend
**Suite:** integration
**Framework:** playwright
**Test Name:** <startup>
**Source File:** tests/integration

### Last Failure Output
```

```

---

## Fix Prompt to Apply

# Fix Prompt for Failing Test frontend/integration/playwright:<startup>

LastRunPassed=false for frontend/integration/playwright:<startup>

## Root Cause
The Playwright suite fails at `<startup>`: its `webServer` command
`docker compose up --build frontend` (see `tests/playwright.config.ts`)
cannot boot because `docker-compose.yaml` builds `./services/frontend`,
and that directory does not exist. No production frontend service has
been created yet.

The test contract (`tests/integration/frontend-home.spec.ts`, READ-ONLY):
```ts
await page.goto('/');
await expect(page.locator('h1')).toHaveText('Hello world');
```
So the service must serve, on port **3000**, a page at `/` whose single
`<h1>` reads exactly `Hello world`.

## Target
Create the Next.js (App Router) production service under
`services/frontend/` ONLY. Do NOT touch anything under `tests/`,
`docs/`, or `true-bdd/`. Do NOT modify the test.

Create each file below with the **Write** tool (all are new files).

## Required Changes

### Change #1: Node/package manifest
**File:** `services/frontend/package.json`
**Action:** Write (new file).
**Content:**
```json
{
  "name": "frontend",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "next dev -p 3000",
    "build": "next build",
    "start": "next start -p 3000 -H 0.0.0.0"
  },
  "dependencies": {
    "next": "14.2.23",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18"
  }
}
```

### Change #2: TypeScript config (referenced by the architecture quality gate)
**File:** `services/frontend/tsconfig.json`
**Action:** Write (new file).
**Content:**
```json
{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": { "@/*": ["./*"] }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

### Change #3: Minimal Next.js config
**File:** `services/frontend/next.config.mjs`
**Action:** Write (new file).
**Content:**
```js
/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;
```

### Change #4: Root layout (App Router requires an html/body root)
**File:** `services/frontend/app/layout.tsx`
**Action:** Write (new file).
**Content:**
```tsx
import type { ReactNode } from 'react';

export const metadata = {
  title: 'Frontend',
};

export default function RootLayout({ children }: { children: ReactNode }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
```

### Change #5: Home page — the single `h1` the test asserts
**File:** `services/frontend/app/page.tsx`
**Action:** Write (new file).
**Content:**
```tsx
export default function Home() {
  return <h1>Hello world</h1>;
}
```
NOTE: This page must render exactly ONE `<h1>` and its full text content
must be `Hello world` (Playwright's `toHaveText('Hello world')` normalizes
whitespace but matches the whole element and fails if more than one `h1`
exists). Do not add extra `h1` elements or surrounding text.

### Change #6: Dockerfile that builds and serves on port 3000
**File:** `services/frontend/Dockerfile`
**Action:** Write (new file).
**Content:**
```dockerfile
FROM node:20-alpine

WORKDIR /app

COPY package.json ./
RUN npm install

COPY . .
RUN npm run build

EXPOSE 3000
CMD ["npm", "run", "start"]
```
NOTE: `docker-compose.yaml` sets `build.context: ./services/frontend`, so
the Dockerfile must live at `services/frontend/Dockerfile` (its default
location). `next start -p 3000 -H 0.0.0.0` binds all interfaces so the
`3000:3000` port mapping reaches it and the Playwright `webServer` URL
`http://127.0.0.1:3000` responds.

### Change #7: Docker ignore (keep host artifacts out of the image build)
**File:** `services/frontend/.dockerignore`
**Action:** Write (new file).
**Content:**
```
node_modules
.next
npm-debug.log
```

## Why this passes
1. `docker compose up --build frontend` now finds `services/frontend/Dockerfile`,
   installs deps, runs `next build`, and starts `next start` on `0.0.0.0:3000`.
2. Compose maps container `3000` → host `3000`; Playwright's `webServer`
   waits on `http://127.0.0.1:3000` and proceeds once it responds.
3. `GET /` serves the App Router home page whose only `<h1>` is
   `Hello world`, satisfying `expect(page.locator('h1')).toHaveText('Hello world')`.

No test assertion is weakened, skipped, or removed; every change is under
`services/frontend/`.

---

## Instructions

1. Use the Read, Glob, and Grep tools to inspect the current state of
   the production source the fix prompt targets.
2. Apply the changes described in the "Fix Prompt to Apply" section
   above:
   - Use Write to create a new file under `services/*` when the fix
     prompt says so.
   - Use Edit to modify an existing file under `services/*` when the
     fix prompt says so.
3. You MAY only modify files under `services/*`.
4. You MUST NOT touch any of the following:
   - Anything under `tests/`
   - Any file matching `*_test.go`
   - Anything under `services/*/__tests__/`
   - Anything under `docs/`
5. Do not weaken, skip, or remove any assertion in the failing test.
   The test is the contract; the production code must conform to it.
6. After the changes succeed, output the confirmation block below.

=== FILE_START: tmp/2026-08-16-21-22-28-21952/apply-frontend-integration-playwright-startup--iter1-result.yaml ===
applied: true
target: "<repo-relative path of the file you wrote/edited>"
summary: "<one-line summary of what changed>"
=== FILE_END: tmp/2026-08-16-21-22-28-21952/apply-frontend-integration-playwright-startup--iter1-result.yaml ===
