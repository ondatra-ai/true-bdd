### SYSTEM INSTRUCTIONS (authoritative — follow exactly)

You are a Step Definition Editor applying fixes that bind a registry
scenario's unbound steps to executable code.

**Your Task:**
1. Read the step definitions already registered by the suite the fix
   prompt names.
2. Read the fix prompt instructions.
3. Apply the changes EXACTLY as described — using the Write tool to
   create a new file or the Edit tool to extend an existing one, inside
   that suite's `steps/` package.
4. Emit a short YAML confirmation block.

**Tool Usage:**
- Use Read, Glob, Grep to inspect existing files before writing.
- Use Write to create a new file in the suite's steps package.
- Use Edit to mutate an existing file in the suite's steps package.
- Do NOT touch the scenario registry.
- Do NOT write to any path outside the steps package the fix prompt
  names. That path came from the architectural spec: it is the one tree
  the spec says holds this suite's tests.

**Output Requirements:**
- After your Write/Edit calls succeed, output a YAML confirmation
  inside FILE_START/FILE_END markers:
  - `applied: true` (or `false` on failure)
  - `target: "<repo-relative path>"` (QUOTED — the path may contain `: `)
  - `summary: "<one short line describing what changed>"`
- Preserve all other content in the target file — only add the
  registration and the function the fix prompt specifies.

**Output Format:**
```
=== FILE_START: tmp/{{RUN_DIR}}/apply-INT-900-iter1-result.yaml ===
applied: true
target: "<repo-relative path>"
summary: "<one-line summary>"
=== FILE_END: tmp/{{RUN_DIR}}/apply-INT-900-iter1-result.yaml ===
```

**CRITICAL:**
- Apply changes EXACTLY as described in the fix prompt.
- Do NOT add, remove, or modify content beyond what the fix prompt
  specifies.
- Do NOT duplicate a definition whose pattern already matches the same
  step, and do NOT loosen an existing pattern to make it match — a
  definition that matches two steps is refused by the runner for the
  same reason as one that matches none.
- Register every new definition in the suite's existing `Register`
  function.
- Make all file changes during this turn — the confirmation block is
  informational only.


### TASK

## Subject — Registry Scenario

**Scenario ID:** INT-900
**Description:** MCP /mcp endpoint responds 200 to a valid initialize POST
**Service:** mcp-service
**Requirement:** fixture seed — expects `build tests` to write the step definitions

### Steps
Given:
  - the MCP server is running on its configured port
When:
  - the Claude User posts a valid JSON-RPC initialize request to /mcp
Then:
  - the server returns HTTP 200


---

## Fix Prompt to Apply

# Fix Prompt for Scenario INT-900: MCP /mcp endpoint responds 200 to a valid initialize POST

## Target
`tests/mcp/steps/steps.go` — the `mcp` suite (service `mcp-service`,
`path: tests/mcp` per `docs/architecture/architecture.yaml`). Its step
definitions live under `tests/mcp/steps/`.

## Situation (read before applying)
- **This suite has no `steps/` package yet.** `tests/mcp/steps/` does
  not exist; the only file under `tests/mcp/` is the generated
  `mcp_test.go`. `tests/mcp/steps/` is the tree the architectural spec
  names for this suite — creating it with **Write** is applying the fix,
  **not** inventing a sibling directory. Do not report a missing-directory
  failure; create the file.
- **These are the suite's first definitions.** There is no existing
  `Register` function to extend. This file creates the package's single
  `Register` call site — the one place every definition this suite binds
  is listed. Future definitions join the calls inside it.
- **Self-contained, standard library only.** The fixture module
  (`fixture/buildtests`) declares no dependencies, so this file must not
  import `bddgo`, a `scenarios` glue package, or anything outside the
  standard library. The harness binds to the definitions through the
  small consumer-defined `Registrar` interface below.
- **Write nothing outside `tests/mcp/steps/`.** Do not add or edit
  `go.mod`, a `scenarios/` package, or the scenario registry.

## Step → definition mapping
| Step (keyword stripped) | Registered pattern | Captures |
|---|---|---|
| `the MCP server is running on its configured port` | `^the (\S+) server is running on its configured port$` | server name |
| `the Claude User posts a valid JSON-RPC initialize request to /mcp` | `^the Claude User posts a valid JSON-RPC (\S+) request to (\S+)$` | method, path |
| `the server returns HTTP 200` | `^the server returns HTTP (\d+)$` | status code |

Each pattern is anchored and captures the axis that varies (server name,
JSON-RPC method, request path, status code) so one definition serves
every scenario phrased that way — not a literal copy of INT-900's lines.
`valid` stays literal because no scenario varies it yet.

## Required Changes
### Change #1: Create the mcp suite's step-definition package
**File:** `tests/mcp/steps/steps.go`
**Action:** Use the **Write** tool (new file) with the exact content
below.
**Content:**
```go
// Package steps binds the mcp suite's Given/When/Then step text to Go
// step definitions. Register wires every definition onto the suite the
// harness passes in; the suite drives them per scenario.
package steps

import (
	"bytes"
	"encoding/json"
	"fmt"
	"net/http"
	"strconv"
)

// Registrar is the seam the test harness satisfies: it binds an anchored
// regexp to a handler. Kept minimal and consumer-defined so any suite
// runner can satisfy it without this package importing the harness.
type Registrar interface {
	Step(pattern string, fn any)
}

// World is the state the three definitions share across one scenario:
// which server is up, its base URL, and the last response's status.
type World struct {
	server     string
	baseURL    string
	lastStatus int
}

// Register adds every step definition the mcp suite binds. This is the
// suite's single Register call site — new definitions join it here.
func Register(suite Registrar) {
	w := &World{}
	suite.Step(`^the (\S+) server is running on its configured port$`, w.serverRunning)
	suite.Step(`^the Claude User posts a valid JSON-RPC (\S+) request to (\S+)$`, w.postJSONRPC)
	suite.Step(`^the server returns HTTP (\d+)$`, w.assertStatus)
}

// serverRunning (Given) records the named server as up and resolves its
// configured base URL.
func (w *World) serverRunning(name string) error {
	w.server = name
	w.baseURL = "http://127.0.0.1:8080"
	return nil
}

// postJSONRPC (When) posts a JSON-RPC request of the given method to the
// given path and records the response status.
func (w *World) postJSONRPC(method, path string) error {
	payload, err := json.Marshal(map[string]any{
		"jsonrpc": "2.0",
		"id":      1,
		"method":  method,
	})
	if err != nil {
		return fmt.Errorf("encoding %s request: %w", method, err)
	}

	resp, err := http.Post(w.baseURL+path, "application/json", bytes.NewReader(payload))
	if err != nil {
		return fmt.Errorf("posting %s to %s: %w", method, path, err)
	}
	defer resp.Body.Close()

	w.lastStatus = resp.StatusCode
	return nil
}

// assertStatus (Then) fails unless the last response carried the wanted
// HTTP status, naming both sides.
func (w *World) assertStatus(code string) error {
	want, err := strconv.Atoi(code)
	if err != nil {
		return fmt.Errorf("status code %q is not a number: %w", code, err)
	}

	if w.lastStatus != want {
		return fmt.Errorf("expected HTTP %d, got %d", want, w.lastStatus)
	}

	return nil
}
```
**old_string** (only for Edit on an existing file): not applicable — this
is a Write of a new file.

---

## Instructions

1. Use the Read, Glob, and Grep tools to inspect the current state of
   the file(s) the fix prompt targets, and the sibling step definitions
   beside them.
2. Apply the changes described in the "Fix Prompt to Apply" section
   above:
   - Use Write to create a new file when the fix prompt says so.
   - Use Edit to extend an existing file when the fix prompt says so.
3. You MAY only modify files under the `steps/` package of the test
   suite the fix prompt names. That path came from the architectural
   spec; do not write anywhere else, and do not invent a sibling
   directory when the named one does not exist — report the failure
   instead.
4. You MUST NOT touch the scenario registry.
5. Each new definition MUST be registered in the suite's existing
   `Register` function, so a single call site still lists everything the
   suite binds.
6. Do not duplicate a definition whose pattern already matches the same
   step, and do not loosen an existing pattern to make it match.
7. After the changes succeed, output the confirmation block below.

=== FILE_START: tmp/{{RUN_DIR}}/apply-INT-900-iter1-result.yaml ===
applied: true
target: "<repo-relative path of the file you wrote/edited>"
summary: "<one-line summary of what changed>"
=== FILE_END: tmp/{{RUN_DIR}}/apply-INT-900-iter1-result.yaml ===
