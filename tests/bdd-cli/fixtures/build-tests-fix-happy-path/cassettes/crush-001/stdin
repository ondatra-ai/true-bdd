### SYSTEM INSTRUCTIONS (authoritative — follow exactly)

You are a Step Definition Editor applying fixes that bind a registry
scenario's unbound steps to executable code.

**Your Task:**
1. Read the step definitions already registered by the suite the fix
   prompt names.
2. Read the fix prompt instructions.
3. Apply the changes EXACTLY as described — using the Write tool to
   create a new file or the Edit tool to extend an existing one, inside
   that suite's `steps/` package.
4. Emit a short YAML confirmation block.

**Tool Usage:**
- Use Read, Glob, Grep to inspect existing files before writing.
- Use Write to create a new file in the suite's steps package.
- Use Edit to mutate an existing file in the suite's steps package.
- Do NOT touch the scenario registry.
- Do NOT write to any path outside the steps package the fix prompt
  names. That path came from the architectural spec: it is the one tree
  the spec says holds this suite's tests.

**Output Requirements:**
- After your Write/Edit calls succeed, output a YAML confirmation
  inside FILE_START/FILE_END markers:
  - `applied: true` (or `false` on failure)
  - `target: "<repo-relative path>"` (QUOTED — the path may contain `: `)
  - `summary: "<one short line describing what changed>"`
- Preserve all other content in the target file — only add the
  registration and the function the fix prompt specifies.

**Output Format:**
```
=== FILE_START: tmp/2026-08-16-21-13-55-16563/apply-INT-900-iter1-result.yaml ===
applied: true
target: "<repo-relative path>"
summary: "<one-line summary>"
=== FILE_END: tmp/2026-08-16-21-13-55-16563/apply-INT-900-iter1-result.yaml ===
```

**CRITICAL:**
- Apply changes EXACTLY as described in the fix prompt.
- Do NOT add, remove, or modify content beyond what the fix prompt
  specifies.
- Do NOT duplicate a definition whose pattern already matches the same
  step, and do NOT loosen an existing pattern to make it match — a
  definition that matches two steps is refused by the runner for the
  same reason as one that matches none.
- Register every new definition in the suite's existing `Register`
  function.
- Make all file changes during this turn — the confirmation block is
  informational only.


### TASK

## Subject — Registry Scenario

**Scenario ID:** INT-900
**Description:** MCP /mcp endpoint responds 200 to a valid initialize POST
**Service:** mcp-service
**Requirement:** fixture seed — expects `build tests` to write the step definitions

### Steps
Given:
  - the MCP server is running on its configured port
When:
  - the Claude User posts a valid JSON-RPC initialize request to /mcp
Then:
  - the server returns HTTP 200


---

## Fix Prompt to Apply

# Fix Prompt for Scenario INT-900: MCP /mcp endpoint responds 200 to a valid initialize POST

## Suite resolution
From `docs/architecture/architecture.yaml`, `architecture.testing.suites[]`
has one entry whose `service:` equals this scenario's Service
(`mcp-service`):

```
- name: mcp
  service: mcp-service
  path: tests/mcp
```

So the suite root is `tests/mcp` and the only writable tree is
**`tests/mcp/steps/`**. That package does not exist yet — no step
definitions, no `Register` function, no sibling state type. This fix
**establishes the package**: it creates the state type and the suite's
single `Register` function, then binds all three of the scenario's
steps in it. Do not write outside `tests/mcp/steps/`, and do not touch
`docs/scenarios.yaml`.

## Step binding plan
| Keyword | Step text | Pattern | Capture |
|---|---|---|---|
| Given | the MCP server is running on its configured port | `^the MCP server is running on its configured port$` | none — no varying token in this precondition, so an anchored literal is the honest pattern |
| When | the Claude User posts a valid JSON-RPC initialize request to /mcp | `^the Claude User posts a valid JSON-RPC (\S+) request to (\S+)$` | method + path — serves `tools/list … to /mcp`, etc. |
| Then | the server returns HTTP 200 | `^the server returns HTTP (\d+)$` | status code — serves 404, 500, … |

Each step matches exactly one pattern and no pattern overlaps another →
the re-walk sees `Verdict: executable`.

## Required Changes

### Change #1: create the suite state type
**File:** `tests/mcp/steps/state.go`
**Action:** Use the **Write** tool to create this new file (the `steps`
package does not exist yet).
**Content:**
```go
package steps

import "net/http"

// State is the per-scenario state the mcp suite keeps: the base URL the
// MCP server is reachable at (prepared by the Given step), and the most
// recent HTTP response the When step produced for the Then step to
// assert against.
type State struct {
	BaseURL  string
	Response *http.Response
}
```

### Change #2: create the suite's single Register function and bind all three steps
**File:** `tests/mcp/steps/steps.go`
**Action:** Use the **Write** tool to create this new file. This is the
one call site that lists everything the suite binds; any future
definition for this suite is added here, not in a second `Register`.
**Content:**
```go
package steps

import (
	"bytes"
	"fmt"
	"net/http"

	"github.com/ondatra-ai/true-bdd/tests/libraries/bddgo"
)

// Register binds every step the mcp suite's scenarios use. Given
// prepares, When acts, Then asserts — each Then error names what was
// expected and what actually happened, because that error IS the test
// failure a reader gets.
func Register(suite *bddgo.Suite[*State]) {
	// Given — precondition. No varying token in the wording, so the
	// pattern is an anchored literal rather than a forced capture group.
	suite.Step(`^the MCP server is running on its configured port$`, func(s *State, m []string) error {
		if s.BaseURL == "" {
			s.BaseURL = "http://127.0.0.1:8080"
		}
		return nil
	})

	// When — act. Method and path vary between phrasings, so both are
	// captured: `initialize … /mcp`, `tools/list … /mcp`, …
	suite.Step(`^the Claude User posts a valid JSON-RPC (\S+) request to (\S+)$`, func(s *State, m []string) error {
		method, path := m[1], m[2]
		body := fmt.Sprintf(`{"jsonrpc":"2.0","id":1,"method":%q,"params":{}}`, method)
		resp, err := http.Post(s.BaseURL+path, "application/json", bytes.NewBufferString(body))
		if err != nil {
			return fmt.Errorf("POST %s%s (%s) failed: %w", s.BaseURL, path, method, err)
		}
		s.Response = resp
		return nil
	})

	// Then — assert. Status code varies, so it is captured.
	suite.Step(`^the server returns HTTP (\d+)$`, func(s *State, m []string) error {
		want := m[1]
		if s.Response == nil {
			return fmt.Errorf("expected HTTP %s but no response was recorded — did the When step run?", want)
		}
		if got := fmt.Sprintf("%d", s.Response.StatusCode); got != want {
			return fmt.Errorf("expected HTTP %s, got HTTP %s", want, got)
		}
		return nil
	})
}
```

## Notes for the applier
- Both files are new; use **Write**, not Edit. There is no existing
  `Register` to extend — this fix creates it.
- Register all three definitions in the single `Register` function
  above; do not split them across multiple registration functions.
- Do not weaken or broaden these patterns later to absorb a different
  step — add a new `suite.Step(...)` line instead.
- Write nothing outside `tests/mcp/steps/`; never modify
  `docs/scenarios.yaml`.

---

## Instructions

1. Use the Read, Glob, and Grep tools to inspect the current state of
   the file(s) the fix prompt targets, and the sibling step definitions
   beside them.
2. Apply the changes described in the "Fix Prompt to Apply" section
   above:
   - Use Write to create a new file when the fix prompt says so.
   - Use Edit to extend an existing file when the fix prompt says so.
3. You MAY only modify files under the `steps/` package of the test
   suite the fix prompt names. That path came from the architectural
   spec; do not write anywhere else, and do not invent a sibling
   directory when the named one does not exist — report the failure
   instead.
4. You MUST NOT touch the scenario registry.
5. Each new definition MUST be registered in the suite's existing
   `Register` function, so a single call site still lists everything the
   suite binds.
6. Do not duplicate a definition whose pattern already matches the same
   step, and do not loosen an existing pattern to make it match.
7. After the changes succeed, output the confirmation block below.

=== FILE_START: tmp/2026-08-16-21-13-55-16563/apply-INT-900-iter1-result.yaml ===
applied: true
target: "<repo-relative path of the file you wrote/edited>"
summary: "<one-line summary of what changed>"
=== FILE_END: tmp/2026-08-16-21-13-55-16563/apply-INT-900-iter1-result.yaml ===
