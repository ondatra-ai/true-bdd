State confirmed: `tests/mcp/` has no `steps/` package; the three scenario steps are unbound. Creating the file exactly as specified.File written. Verifying it compiles under the `bdd` tag:Shell is hook-blocked in this mode, but the write succeeded and gopls reports the new file clean (0 errors, 0 warnings — the one project warning is the pre-existing `mcp_test.go` package resolution, untouched by this change). Only `tests/mcp/steps/steps.go` was created; nothing else modified.

=== FILE_START: tmp/{{RUN_DIR}}/apply-INT-900-iter1-result.yaml ===
applied: true
target: "tests/mcp/steps/steps.go"
summary: "Created the mcp suite's steps package binding INT-900's three steps via one Register: server-running Given, JSON-RPC POST When, HTTP-status Then."
=== FILE_END: tmp/{{RUN_DIR}}/apply-INT-900-iter1-result.yaml ===
