{
  "//": "Static typecheck gate for the E2E suite (design-conformance-tests scaffolding). Playwright transpiles specs WITHOUT type-checking, so `npx tsc --noEmit` here catches wrong import paths / missing named exports before a spec explodes at its call site. Non-strict on purpose: this gate is about resolution + signature correctness across the package, not null-strictness of the pre-existing specs.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "types": ["node"],
    "strict": false,
    "noEmit": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "forceConsistentCasingInFileNames": true
  },
  "include": ["**/*.ts"],
  "exclude": ["node_modules", "fixtures", "test-results"]
}
