{
  "type": "object",
  "additionalProperties": false,
  "required": ["task_summary", "operations"],
  "properties": {
    "task_summary": { "type": "string" },
    "operations": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["action", "perspective", "requirement", "match"],
        "properties": {
          "action": { "enum": ["add", "update", "delete"] },
          "perspective": { "enum": ["harness", "system", "product"] },
          "requirement": { "type": ["string", "null"] },
          "match": { "type": ["string", "null"] }
        }
      }
    }
  }
}
