---
description: Start a new task — the repo resets to a clean up-to-date state on the current branch and history rolls over on the next prompt
allowed-tools: Bash(*)
---
!`"${CLAUDE_PROJECT_DIR:-.}/.claude/commands/new-task.sh"`
