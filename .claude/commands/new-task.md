---
description: Start a new task — history rolls over so the next prompt opens a fresh file. Leaves the repo and the current branch untouched.
allowed-tools: Bash(*)
---
!`"${CLAUDE_PROJECT_DIR:-.}/.claude/commands/new-task.sh"`
