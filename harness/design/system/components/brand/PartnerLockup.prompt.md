One-line: the sanctioned co-branding composition — S&F × Partner in a single balanced horizontal lockup.

```jsx
<PartnerLockup partner="Lowe's Innovation Labs" size={40} />
```

- Both names carry equal weight and size; the "×" is smaller and half-opacity.
- Never stack vertically, never scale one mark larger than the other, never add a divider rule.
