One-line: a small uppercase label for categories, states, and metadata.

```jsx
<Tag>Research</Tag>
<Tag tone="success">Live</Tag>
```

- `error`/`success`/`warning` map to the three semantic colours — the only sanctioned use of red/green/yellow.
- `inverse` on black bands, `solid` when a tag needs to read as a chip.
